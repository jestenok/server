apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clustersecretstores.external-secrets.io
  uid: 8c350cc9-1df4-42bc-8877-e760d0630dc5
  resourceVersion: '2044646'
  generation: 2
  creationTimestamp: '2023-03-29T16:16:10Z'
  labels:
    app.kubernetes.io/managed-by: Helm
    helm.toolkit.fluxcd.io/name: external-secrets
    helm.toolkit.fluxcd.io/namespace: flux-system
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
    meta.helm.sh/release-name: external-secrets-external-secrets
    meta.helm.sh/release-namespace: external-secrets
  managedFields:
    - manager: helm-controller
      operation: Update
      apiVersion: apiextensions.k8s.io/v1
      time: '2023-03-29T16:16:10Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:controller-gen.kubebuilder.io/version: {}
            f:meta.helm.sh/release-name: {}
            f:meta.helm.sh/release-namespace: {}
          f:labels:
            .: {}
            f:app.kubernetes.io/managed-by: {}
            f:helm.toolkit.fluxcd.io/name: {}
            f:helm.toolkit.fluxcd.io/namespace: {}
        f:spec:
          f:conversion:
            .: {}
            f:strategy: {}
            f:webhook:
              .: {}
              f:clientConfig:
                .: {}
                f:service:
                  .: {}
                  f:name: {}
                  f:namespace: {}
                  f:path: {}
                  f:port: {}
              f:conversionReviewVersions: {}
          f:group: {}
          f:names:
            f:categories: {}
            f:kind: {}
            f:listKind: {}
            f:plural: {}
            f:shortNames: {}
            f:singular: {}
          f:scope: {}
          f:versions: {}
    - manager: k3s
      operation: Update
      apiVersion: apiextensions.k8s.io/v1
      time: '2023-03-29T16:16:11Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:status:
          f:acceptedNames:
            f:categories: {}
            f:kind: {}
            f:listKind: {}
            f:plural: {}
            f:shortNames: {}
            f:singular: {}
          f:conditions:
            k:{"type":"Established"}:
              .: {}
              f:lastTransitionTime: {}
              f:message: {}
              f:reason: {}
              f:status: {}
              f:type: {}
            k:{"type":"NamesAccepted"}:
              .: {}
              f:lastTransitionTime: {}
              f:message: {}
              f:reason: {}
              f:status: {}
              f:type: {}
      subresource: status
    - manager: external-secrets
      operation: Update
      apiVersion: apiextensions.k8s.io/v1
      time: '2023-03-29T16:16:14Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:spec:
          f:conversion:
            f:webhook:
              f:clientConfig:
                f:caBundle: {}
  selfLink: >-
    /apis/apiextensions.k8s.io/v1/customresourcedefinitions/clustersecretstores.external-secrets.io
status:
  conditions:
    - type: NamesAccepted
      status: 'True'
      lastTransitionTime: '2023-03-29T16:16:11Z'
      reason: NoConflicts
      message: no conflicts found
    - type: Established
      status: 'True'
      lastTransitionTime: '2023-03-29T16:16:11Z'
      reason: InitialNamesAccepted
      message: the initial names have been accepted
  acceptedNames:
    plural: clustersecretstores
    singular: clustersecretstore
    shortNames:
      - css
    kind: ClusterSecretStore
    listKind: ClusterSecretStoreList
    categories:
      - externalsecrets
  storedVersions:
    - v1beta1
spec:
  group: external-secrets.io
  names:
    plural: clustersecretstores
    singular: clustersecretstore
    shortNames:
      - css
    kind: ClusterSecretStore
    listKind: ClusterSecretStoreList
    categories:
      - externalsecrets
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      storage: false
      deprecated: true
      schema:
        openAPIV3Schema:
          description: >-
            ClusterSecretStore represents a secure external location for storing
            secrets, which can be referenced as part of `storeRef` fields.
          type: object
          properties:
            apiVersion:
              description: >-
                APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the
                latest internal value, and may reject unrecognized values. More
                info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: >-
                Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase.
                More info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: SecretStoreSpec defines the desired state of SecretStore.
              type: object
              required:
                - provider
              properties:
                controller:
                  description: >-
                    Used to select the correct KES controller (think:
                    ingress.ingressClassName) The KES controller is instantiated
                    with a specific controller name and filters ES based on this
                    property
                  type: string
                provider:
                  description: Used to configure the provider. Only one provider may be set
                  type: object
                  maxProperties: 1
                  minProperties: 1
                  properties:
                    akeyless:
                      description: >-
                        Akeyless configures this store to sync secrets using
                        Akeyless Vault provider
                      type: object
                      required:
                        - akeylessGWApiURL
                        - authSecretRef
                      properties:
                        akeylessGWApiURL:
                          description: >-
                            Akeyless GW API Url from which the secrets to be
                            fetched from.
                          type: string
                        authSecretRef:
                          description: >-
                            Auth configures how the operator authenticates with
                            Akeyless.
                          type: object
                          properties:
                            kubernetesAuth:
                              description: >-
                                Kubernetes authenticates with Akeyless by
                                passing the ServiceAccount token stored in the
                                named Secret resource.
                              type: object
                              required:
                                - accessID
                                - k8sConfName
                              properties:
                                accessID:
                                  description: >-
                                    the Akeyless Kubernetes auth-method
                                    access-id
                                  type: string
                                k8sConfName:
                                  description: >-
                                    Kubernetes-auth configuration name in
                                    Akeyless-Gateway
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional secret field containing a
                                    Kubernetes ServiceAccount JWT used for
                                    authenticating with Akeyless. If a name is
                                    specified without a key, `token` is the
                                    default. If one is not specified, the one
                                    bound to the controller will be used.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                serviceAccountRef:
                                  description: >-
                                    Optional service account field containing
                                    the name of a kubernetes ServiceAccount. If
                                    the service account is specified, the
                                    service account secret token JWT will be
                                    used for authenticating with Akeyless. If
                                    the service account selector is not
                                    supplied, the secretRef will be used
                                    instead.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            secretRef:
                              description: >-
                                Reference to a Secret that contains the details
                                to authenticate with Akeyless.
                              type: object
                              properties:
                                accessID:
                                  description: >-
                                    The SecretAccessID is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessType:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessTypeParam:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        caBundle:
                          description: >-
                            PEM/base64 encoded CA bundle used to validate
                            Akeyless Gateway certificate. Only used if the
                            AkeylessGWApiURL URL is using HTTPS protocol. If not
                            set the system root certificates are used to
                            validate the TLS connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Akeyless Gateway certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key the value inside of the provider type to
                                use, only used with "Secret" type
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: The namespace the Provider type is in.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                    alibaba:
                      description: >-
                        Alibaba configures this store to sync secrets using
                        Alibaba Cloud provider
                      type: object
                      required:
                        - auth
                        - regionID
                      properties:
                        auth:
                          description: AlibabaAuth contains a secretRef for credentials.
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              description: >-
                                AlibabaAuthSecretRef holds secret references for
                                Alibaba credentials.
                              type: object
                              required:
                                - accessKeyIDSecretRef
                                - accessKeySecretSecretRef
                              properties:
                                accessKeyIDSecretRef:
                                  description: The AccessKeyID is used for authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessKeySecretSecretRef:
                                  description: >-
                                    The AccessKeySecret is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        endpoint:
                          type: string
                        regionID:
                          description: Alibaba Region to be used for the provider
                          type: string
                    aws:
                      description: >-
                        AWS configures this store to sync secrets using AWS
                        Secret Manager provider
                      type: object
                      required:
                        - region
                        - service
                      properties:
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against AWS if not set aws sdk will
                            infer credentials from your environment see:
                            https://docs.aws.amazon.com/sdk-for-go/v1/developer-guide/configuring-sdk.html#specifying-credentials
                          type: object
                          properties:
                            jwt:
                              description: >-
                                Authenticate against AWS using service account
                                tokens.
                              type: object
                              properties:
                                serviceAccountRef:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            secretRef:
                              description: >-
                                AWSAuthSecretRef holds secret references for AWS
                                credentials both AccessKeyID and SecretAccessKey
                                must be defined in order to properly
                                authenticate.
                              type: object
                              properties:
                                accessKeyIDSecretRef:
                                  description: The AccessKeyID is used for authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                secretAccessKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        region:
                          description: AWS Region to be used for the provider
                          type: string
                        role:
                          description: >-
                            Role is a Role ARN which the SecretManager provider
                            will assume
                          type: string
                        service:
                          description: >-
                            Service defines which service should be used to
                            fetch the secrets
                          type: string
                          enum:
                            - SecretsManager
                            - ParameterStore
                    azurekv:
                      description: >-
                        AzureKV configures this store to sync secrets using
                        Azure Key Vault provider
                      type: object
                      required:
                        - vaultUrl
                      properties:
                        authSecretRef:
                          description: >-
                            Auth configures how the operator authenticates with
                            Azure. Required for ServicePrincipal auth type.
                          type: object
                          properties:
                            clientId:
                              description: >-
                                The Azure clientId of the service principle used
                                for authentication.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            clientSecret:
                              description: >-
                                The Azure ClientSecret of the service principle
                                used for authentication.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        authType:
                          description: >-
                            Auth type defines how to authenticate to the
                            keyvault service. Valid values are: -
                            "ServicePrincipal" (default): Using a service
                            principal (tenantId, clientId, clientSecret) -
                            "ManagedIdentity": Using Managed Identity assigned
                            to the pod (see aad-pod-identity)
                          type: string
                          default: ServicePrincipal
                          enum:
                            - ServicePrincipal
                            - ManagedIdentity
                            - WorkloadIdentity
                        identityId:
                          description: >-
                            If multiple Managed Identity is assigned to the pod,
                            you can select the one to be used
                          type: string
                        serviceAccountRef:
                          description: >-
                            ServiceAccountRef specified the service account that
                            should be used when authenticating with
                            WorkloadIdentity.
                          type: object
                          required:
                            - name
                          properties:
                            audiences:
                              description: >-
                                Audience specifies the `aud` claim for the
                                service account token If the service account
                                uses a well-known annotation for e.g. IRSA or
                                GCP Workload Identity then this audiences will
                                be appended to the list
                              type: array
                              items:
                                type: string
                            name:
                              description: >-
                                The name of the ServiceAccount resource being
                                referred to.
                              type: string
                            namespace:
                              description: >-
                                Namespace of the resource being referred to.
                                Ignored if referent is not cluster-scoped.
                                cluster-scoped defaults to the namespace of the
                                referent.
                              type: string
                        tenantId:
                          description: >-
                            TenantID configures the Azure Tenant to send
                            requests to. Required for ServicePrincipal auth
                            type.
                          type: string
                        vaultUrl:
                          description: Vault Url from which the secrets to be fetched from.
                          type: string
                    fake:
                      description: Fake configures a store with static key/value pairs
                      type: object
                      required:
                        - data
                      properties:
                        data:
                          type: array
                          items:
                            type: object
                            required:
                              - key
                            properties:
                              key:
                                type: string
                              value:
                                type: string
                              valueMap:
                                type: object
                                additionalProperties:
                                  type: string
                              version:
                                type: string
                    gcpsm:
                      description: >-
                        GCPSM configures this store to sync secrets using Google
                        Cloud Platform Secret Manager provider
                      type: object
                      properties:
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against GCP
                          type: object
                          properties:
                            secretRef:
                              type: object
                              properties:
                                secretAccessKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            workloadIdentity:
                              type: object
                              required:
                                - clusterLocation
                                - clusterName
                                - serviceAccountRef
                              properties:
                                clusterLocation:
                                  type: string
                                clusterName:
                                  type: string
                                clusterProjectID:
                                  type: string
                                serviceAccountRef:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        projectID:
                          description: ProjectID project where secret is located
                          type: string
                    gitlab:
                      description: >-
                        Gitlab configures this store to sync secrets using
                        Gitlab Variables provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with a GitLab instance.
                          type: object
                          required:
                            - SecretRef
                          properties:
                            SecretRef:
                              type: object
                              properties:
                                accessToken:
                                  description: AccessToken is used for authentication.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        projectID:
                          description: >-
                            ProjectID specifies a project where secrets are
                            located.
                          type: string
                        url:
                          description: >-
                            URL configures the GitLab instance URL. Defaults to
                            https://gitlab.com/.
                          type: string
                    ibm:
                      description: >-
                        IBM configures this store to sync secrets using IBM
                        Cloud provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the IBM secrets manager.
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              type: object
                              properties:
                                secretApiKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        serviceUrl:
                          description: >-
                            ServiceURL is the Endpoint URL that is specific to
                            the Secrets Manager service instance
                          type: string
                    kubernetes:
                      description: >-
                        Kubernetes configures this store to sync secrets using a
                        Kubernetes cluster provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with a Kubernetes instance.
                          type: object
                          maxProperties: 1
                          minProperties: 1
                          properties:
                            cert:
                              description: >-
                                has both clientCert and clientKey as
                                secretKeySelector
                              type: object
                              properties:
                                clientCert:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                clientKey:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            serviceAccount:
                              description: >-
                                points to a service account that should be used
                                for authentication
                              type: object
                              properties:
                                serviceAccount:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            token:
                              description: use static token to authenticate with
                              type: object
                              properties:
                                bearerToken:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        remoteNamespace:
                          description: Remote namespace to fetch the secrets from
                          type: string
                          default: default
                        server:
                          description: configures the Kubernetes server Address.
                          type: object
                          properties:
                            caBundle:
                              description: CABundle is a base64-encoded CA certificate
                              type: string
                              format: byte
                            caProvider:
                              description: >-
                                see:
                                https://external-secrets.io/v0.4.1/spec/#external-secrets.io/v1alpha1.CAProvider
                              type: object
                              required:
                                - name
                                - type
                              properties:
                                key:
                                  description: >-
                                    The key the value inside of the provider
                                    type to use, only used with "Secret" type
                                  type: string
                                name:
                                  description: >-
                                    The name of the object located at the
                                    provider type.
                                  type: string
                                namespace:
                                  description: The namespace the Provider type is in.
                                  type: string
                                type:
                                  description: >-
                                    The type of provider to use such as
                                    "Secret", or "ConfigMap".
                                  type: string
                                  enum:
                                    - Secret
                                    - ConfigMap
                            url:
                              description: configures the Kubernetes server Address.
                              type: string
                              default: kubernetes.default
                    oracle:
                      description: >-
                        Oracle configures this store to sync secrets using
                        Oracle Vault provider
                      type: object
                      required:
                        - region
                        - vault
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the Oracle Vault. If empty, use the instance
                            principal, otherwise the user credentials specified
                            in Auth.
                          type: object
                          required:
                            - secretRef
                            - tenancy
                            - user
                          properties:
                            secretRef:
                              description: SecretRef to pass through sensitive information.
                              type: object
                              required:
                                - fingerprint
                                - privatekey
                              properties:
                                fingerprint:
                                  description: >-
                                    Fingerprint is the fingerprint of the API
                                    private key.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                privatekey:
                                  description: >-
                                    PrivateKey is the user's API Signing Key in
                                    PEM format, used for authentication.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            tenancy:
                              description: >-
                                Tenancy is the tenancy OCID where user is
                                located.
                              type: string
                            user:
                              description: User is an access OCID specific to the account.
                              type: string
                        region:
                          description: Region is the region where vault is located.
                          type: string
                        vault:
                          description: >-
                            Vault is the vault's OCID of the specific vault
                            where secret is located.
                          type: string
                    vault:
                      description: >-
                        Vault configures this store to sync secrets using Hashi
                        provider
                      type: object
                      required:
                        - auth
                        - server
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the Vault server.
                          type: object
                          properties:
                            appRole:
                              description: >-
                                AppRole authenticates with Vault using the App
                                Role auth mechanism, with the role and secret
                                stored in a Kubernetes Secret resource.
                              type: object
                              required:
                                - path
                                - roleId
                                - secretRef
                              properties:
                                path:
                                  description: >-
                                    Path where the App Role authentication
                                    backend is mounted in Vault, e.g: "approle"
                                  type: string
                                  default: approle
                                roleId:
                                  description: >-
                                    RoleID configured in the App Role
                                    authentication backend when setting up the
                                    authentication backend in Vault.
                                  type: string
                                secretRef:
                                  description: >-
                                    Reference to a key in a Secret that contains
                                    the App Role secret used to authenticate
                                    with Vault. The `key` field must be
                                    specified and denotes which entry within the
                                    Secret resource is used as the app role
                                    secret.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            cert:
                              description: >-
                                Cert authenticates with TLS Certificates by
                                passing client certificate, private key and ca
                                certificate Cert authentication method
                              type: object
                              properties:
                                clientCert:
                                  description: >-
                                    ClientCert is a certificate to authenticate
                                    using the Cert Vault authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                secretRef:
                                  description: >-
                                    SecretRef to a key in a Secret resource
                                    containing client private key to
                                    authenticate with Vault using the Cert
                                    authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            jwt:
                              description: >-
                                Jwt authenticates with Vault by passing role and
                                JWT token using the JWT/OIDC authentication
                                method
                              type: object
                              required:
                                - path
                              properties:
                                kubernetesServiceAccountToken:
                                  description: >-
                                    Optional ServiceAccountToken specifies the
                                    Kubernetes service account for which to
                                    request a token for with the `TokenRequest`
                                    API.
                                  type: object
                                  required:
                                    - serviceAccountRef
                                  properties:
                                    audiences:
                                      description: >-
                                        Optional audiences field that will be
                                        used to request a temporary Kubernetes
                                        service account token for the service
                                        account referenced by
                                        `serviceAccountRef`. Defaults to a
                                        single audience `vault` it not
                                        specified.
                                      type: array
                                      items:
                                        type: string
                                    expirationSeconds:
                                      description: >-
                                        Optional expiration time in seconds that
                                        will be used to request a temporary
                                        Kubernetes service account token for the
                                        service account referenced by
                                        `serviceAccountRef`. Defaults to 10
                                        minutes.
                                      type: integer
                                      format: int64
                                    serviceAccountRef:
                                      description: >-
                                        Service account field containing the
                                        name of a kubernetes ServiceAccount.
                                      type: object
                                      required:
                                        - name
                                      properties:
                                        audiences:
                                          description: >-
                                            Audience specifies the `aud` claim for
                                            the service account token If the service
                                            account uses a well-known annotation for
                                            e.g. IRSA or GCP Workload Identity then
                                            this audiences will be appended to the
                                            list
                                          type: array
                                          items:
                                            type: string
                                        name:
                                          description: >-
                                            The name of the ServiceAccount resource
                                            being referred to.
                                          type: string
                                        namespace:
                                          description: >-
                                            Namespace of the resource being referred
                                            to. Ignored if referent is not
                                            cluster-scoped. cluster-scoped defaults
                                            to the namespace of the referent.
                                          type: string
                                path:
                                  description: >-
                                    Path where the JWT authentication backend is
                                    mounted in Vault, e.g: "jwt"
                                  type: string
                                  default: jwt
                                role:
                                  description: >-
                                    Role is a JWT role to authenticate using the
                                    JWT/OIDC Vault authentication method
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional SecretRef that refers to a key in a
                                    Secret resource containing JWT token to
                                    authenticate with Vault using the JWT/OIDC
                                    authentication method.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            kubernetes:
                              description: >-
                                Kubernetes authenticates with Vault by passing
                                the ServiceAccount token stored in the named
                                Secret resource to the Vault server.
                              type: object
                              required:
                                - mountPath
                                - role
                              properties:
                                mountPath:
                                  description: >-
                                    Path where the Kubernetes authentication
                                    backend is mounted in Vault, e.g:
                                    "kubernetes"
                                  type: string
                                  default: kubernetes
                                role:
                                  description: >-
                                    A required field containing the Vault Role
                                    to assume. A Role binds a Kubernetes
                                    ServiceAccount with a set of Vault policies.
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional secret field containing a
                                    Kubernetes ServiceAccount JWT used for
                                    authenticating with Vault. If a name is
                                    specified without a key, `token` is the
                                    default. If one is not specified, the one
                                    bound to the controller will be used.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                serviceAccountRef:
                                  description: >-
                                    Optional service account field containing
                                    the name of a kubernetes ServiceAccount. If
                                    the service account is specified, the
                                    service account secret token JWT will be
                                    used for authenticating with Vault. If the
                                    service account selector is not supplied,
                                    the secretRef will be used instead.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            ldap:
                              description: >-
                                Ldap authenticates with Vault by passing
                                username/password pair using the LDAP
                                authentication method
                              type: object
                              required:
                                - path
                                - username
                              properties:
                                path:
                                  description: >-
                                    Path where the LDAP authentication backend
                                    is mounted in Vault, e.g: "ldap"
                                  type: string
                                  default: ldap
                                secretRef:
                                  description: >-
                                    SecretRef to a key in a Secret resource
                                    containing password for the LDAP user used
                                    to authenticate with Vault using the LDAP
                                    authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                username:
                                  description: >-
                                    Username is a LDAP user name used to
                                    authenticate using the LDAP Vault
                                    authentication method
                                  type: string
                            tokenSecretRef:
                              description: >-
                                TokenSecretRef authenticates with Vault by
                                presenting a token.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caBundle:
                          description: >-
                            PEM encoded CA bundle used to validate Vault server
                            certificate. Only used if the Server URL is using
                            HTTPS protocol. This parameter is ignored for plain
                            HTTP protocol connection. If not set the system root
                            certificates are used to validate the TLS
                            connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Vault server certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key the value inside of the provider type to
                                use, only used with "Secret" type
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: The namespace the Provider type is in.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                        forwardInconsistent:
                          description: >-
                            ForwardInconsistent tells Vault to forward
                            read-after-write requests to the Vault leader
                            instead of simply retrying within a loop. This can
                            increase performance if the option is enabled
                            serverside.
                            https://www.vaultproject.io/docs/configuration/replication#allow_forwarding_via_header
                          type: boolean
                        namespace:
                          description: >-
                            Name of the vault namespace. Namespaces is a set of
                            features within Vault Enterprise that allows Vault
                            environments to support Secure Multi-tenancy. e.g:
                            "ns1". More about namespaces can be found here
                            https://www.vaultproject.io/docs/enterprise/namespaces
                          type: string
                        path:
                          description: >-
                            Path is the mount path of the Vault KV backend
                            endpoint, e.g: "secret". The v2 KV secret engine
                            version specific "/data" path suffix for fetching
                            secrets from Vault is optional and will be appended
                            if not present in specified path.
                          type: string
                        readYourWrites:
                          description: >-
                            ReadYourWrites ensures isolated read-after-write
                            semantics by providing discovered cluster
                            replication states in each request. More information
                            about eventual consistency in Vault can be found
                            here
                            https://www.vaultproject.io/docs/enterprise/consistency
                          type: boolean
                        server:
                          description: >-
                            Server is the connection address for the Vault
                            server, e.g: "https://vault.example.com:8200".
                          type: string
                        version:
                          description: >-
                            Version is the Vault KV secret engine version. This
                            can be either "v1" or "v2". Version defaults to
                            "v2".
                          type: string
                          default: v2
                          enum:
                            - v1
                            - v2
                    webhook:
                      description: >-
                        Webhook configures this store to sync secrets using a
                        generic templated webhook
                      type: object
                      required:
                        - result
                        - url
                      properties:
                        body:
                          description: Body
                          type: string
                        caBundle:
                          description: >-
                            PEM encoded CA bundle used to validate webhook
                            server certificate. Only used if the Server URL is
                            using HTTPS protocol. This parameter is ignored for
                            plain HTTP protocol connection. If not set the
                            system root certificates are used to validate the
                            TLS connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            webhook server certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key the value inside of the provider type to
                                use, only used with "Secret" type
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: The namespace the Provider type is in.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                        headers:
                          description: Headers
                          type: object
                          additionalProperties:
                            type: string
                        method:
                          description: Webhook Method
                          type: string
                        result:
                          description: Result formatting
                          type: object
                          properties:
                            jsonPath:
                              description: Json path of return value
                              type: string
                        secrets:
                          description: >-
                            Secrets to fill in templates These secrets will be
                            passed to the templating function as key value pairs
                            under the given name
                          type: array
                          items:
                            type: object
                            required:
                              - name
                              - secretRef
                            properties:
                              name:
                                description: Name of this secret in templates
                                type: string
                              secretRef:
                                description: Secret ref to fill in credentials
                                type: object
                                properties:
                                  key:
                                    description: >-
                                      The key of the entry in the Secret
                                      resource's `data` field to be used. Some
                                      instances of this field may be defaulted,
                                      in others it may be required.
                                    type: string
                                  name:
                                    description: >-
                                      The name of the Secret resource being
                                      referred to.
                                    type: string
                                  namespace:
                                    description: >-
                                      Namespace of the resource being referred
                                      to. Ignored if referent is not
                                      cluster-scoped. cluster-scoped defaults to
                                      the namespace of the referent.
                                    type: string
                        timeout:
                          description: Timeout
                          type: string
                        url:
                          description: Webhook url to call
                          type: string
                    yandexlockbox:
                      description: >-
                        YandexLockbox configures this store to sync secrets
                        using Yandex Lockbox provider
                      type: object
                      required:
                        - auth
                      properties:
                        apiEndpoint:
                          description: >-
                            Yandex.Cloud API endpoint (e.g.
                            'api.cloud.yandex.net:443')
                          type: string
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against Yandex Lockbox
                          type: object
                          properties:
                            authorizedKeySecretRef:
                              description: The authorized key used for authentication
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Yandex.Cloud server certificate.
                          type: object
                          properties:
                            certSecretRef:
                              description: >-
                                A reference to a specific 'key' within a Secret
                                resource, In some instances, `key` is a required
                                field.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                retrySettings:
                  description: Used to configure http retries if failed
                  type: object
                  properties:
                    maxRetries:
                      type: integer
                      format: int32
                    retryInterval:
                      type: string
            status:
              description: SecretStoreStatus defines the observed state of the SecretStore.
              type: object
              properties:
                conditions:
                  type: array
                  items:
                    type: object
                    required:
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        type: string
                        format: date-time
                      message:
                        type: string
                      reason:
                        type: string
                      status:
                        type: string
                      type:
                        type: string
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: AGE
          type: date
          jsonPath: .metadata.creationTimestamp
        - name: Status
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].reason
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          description: >-
            ClusterSecretStore represents a secure external location for storing
            secrets, which can be referenced as part of `storeRef` fields.
          type: object
          properties:
            apiVersion:
              description: >-
                APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the
                latest internal value, and may reject unrecognized values. More
                info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: >-
                Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase.
                More info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: SecretStoreSpec defines the desired state of SecretStore.
              type: object
              required:
                - provider
              properties:
                conditions:
                  description: >-
                    Used to constraint a ClusterSecretStore to specific
                    namespaces. Relevant only to ClusterSecretStore
                  type: array
                  items:
                    description: >-
                      ClusterSecretStoreCondition describes a condition by which
                      to choose namespaces to process ExternalSecrets in for a
                      ClusterSecretStore instance.
                    type: object
                    properties:
                      namespaceSelector:
                        description: Choose namespace using a labelSelector
                        type: object
                        properties:
                          matchExpressions:
                            description: >-
                              matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            type: array
                            items:
                              description: >-
                                A label selector requirement is a selector that
                                contains values, a key, and an operator that
                                relates the key and values.
                              type: object
                              required:
                                - key
                                - operator
                              properties:
                                key:
                                  description: >-
                                    key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: >-
                                    operator represents a key's relationship to
                                    a set of values. Valid operators are In,
                                    NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: >-
                                    values is an array of string values. If the
                                    operator is In or NotIn, the values array
                                    must be non-empty. If the operator is Exists
                                    or DoesNotExist, the values array must be
                                    empty. This array is replaced during a
                                    strategic merge patch.
                                  type: array
                                  items:
                                    type: string
                          matchLabels:
                            description: >-
                              matchLabels is a map of {key,value} pairs. A
                              single {key,value} in the matchLabels map is
                              equivalent to an element of matchExpressions,
                              whose key field is "key", the operator is "In",
                              and the values array contains only "value". The
                              requirements are ANDed.
                            type: object
                            additionalProperties:
                              type: string
                        x-kubernetes-map-type: atomic
                      namespaces:
                        description: Choose namespaces by name
                        type: array
                        items:
                          type: string
                controller:
                  description: >-
                    Used to select the correct KES controller (think:
                    ingress.ingressClassName) The KES controller is instantiated
                    with a specific controller name and filters ES based on this
                    property
                  type: string
                provider:
                  description: Used to configure the provider. Only one provider may be set
                  type: object
                  maxProperties: 1
                  minProperties: 1
                  properties:
                    akeyless:
                      description: >-
                        Akeyless configures this store to sync secrets using
                        Akeyless Vault provider
                      type: object
                      required:
                        - akeylessGWApiURL
                        - authSecretRef
                      properties:
                        akeylessGWApiURL:
                          description: >-
                            Akeyless GW API Url from which the secrets to be
                            fetched from.
                          type: string
                        authSecretRef:
                          description: >-
                            Auth configures how the operator authenticates with
                            Akeyless.
                          type: object
                          properties:
                            kubernetesAuth:
                              description: >-
                                Kubernetes authenticates with Akeyless by
                                passing the ServiceAccount token stored in the
                                named Secret resource.
                              type: object
                              required:
                                - accessID
                                - k8sConfName
                              properties:
                                accessID:
                                  description: >-
                                    the Akeyless Kubernetes auth-method
                                    access-id
                                  type: string
                                k8sConfName:
                                  description: >-
                                    Kubernetes-auth configuration name in
                                    Akeyless-Gateway
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional secret field containing a
                                    Kubernetes ServiceAccount JWT used for
                                    authenticating with Akeyless. If a name is
                                    specified without a key, `token` is the
                                    default. If one is not specified, the one
                                    bound to the controller will be used.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                serviceAccountRef:
                                  description: >-
                                    Optional service account field containing
                                    the name of a kubernetes ServiceAccount. If
                                    the service account is specified, the
                                    service account secret token JWT will be
                                    used for authenticating with Akeyless. If
                                    the service account selector is not
                                    supplied, the secretRef will be used
                                    instead.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            secretRef:
                              description: >-
                                Reference to a Secret that contains the details
                                to authenticate with Akeyless.
                              type: object
                              properties:
                                accessID:
                                  description: >-
                                    The SecretAccessID is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessType:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessTypeParam:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        caBundle:
                          description: >-
                            PEM/base64 encoded CA bundle used to validate
                            Akeyless Gateway certificate. Only used if the
                            AkeylessGWApiURL URL is using HTTPS protocol. If not
                            set the system root certificates are used to
                            validate the TLS connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Akeyless Gateway certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key where the CA certificate can be found in
                                the Secret or ConfigMap.
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: >-
                                The namespace the Provider type is in. Can only
                                be defined when used in a ClusterSecretStore.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                    alibaba:
                      description: >-
                        Alibaba configures this store to sync secrets using
                        Alibaba Cloud provider
                      type: object
                      required:
                        - auth
                        - regionID
                      properties:
                        auth:
                          description: AlibabaAuth contains a secretRef for credentials.
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              description: >-
                                AlibabaAuthSecretRef holds secret references for
                                Alibaba credentials.
                              type: object
                              required:
                                - accessKeyIDSecretRef
                                - accessKeySecretSecretRef
                              properties:
                                accessKeyIDSecretRef:
                                  description: The AccessKeyID is used for authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessKeySecretSecretRef:
                                  description: >-
                                    The AccessKeySecret is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        endpoint:
                          type: string
                        regionID:
                          description: Alibaba Region to be used for the provider
                          type: string
                    aws:
                      description: >-
                        AWS configures this store to sync secrets using AWS
                        Secret Manager provider
                      type: object
                      required:
                        - region
                        - service
                      properties:
                        additionalRoles:
                          description: >-
                            AdditionalRoles is a chained list of Role ARNs which
                            the SecretManager provider will sequentially assume
                            before assuming Role
                          type: array
                          items:
                            type: string
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against AWS if not set aws sdk will
                            infer credentials from your environment see:
                            https://docs.aws.amazon.com/sdk-for-go/v1/developer-guide/configuring-sdk.html#specifying-credentials
                          type: object
                          properties:
                            jwt:
                              description: >-
                                Authenticate against AWS using service account
                                tokens.
                              type: object
                              properties:
                                serviceAccountRef:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            secretRef:
                              description: >-
                                AWSAuthSecretRef holds secret references for AWS
                                credentials both AccessKeyID and SecretAccessKey
                                must be defined in order to properly
                                authenticate.
                              type: object
                              properties:
                                accessKeyIDSecretRef:
                                  description: The AccessKeyID is used for authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                secretAccessKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                sessionTokenSecretRef:
                                  description: >-
                                    The SessionToken used for authentication
                                    This must be defined if AccessKeyID and
                                    SecretAccessKey are temporary credentials
                                    see:
                                    https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_use-resources.html
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        region:
                          description: AWS Region to be used for the provider
                          type: string
                        role:
                          description: >-
                            Role is a Role ARN which the SecretManager provider
                            will assume
                          type: string
                        service:
                          description: >-
                            Service defines which service should be used to
                            fetch the secrets
                          type: string
                          enum:
                            - SecretsManager
                            - ParameterStore
                    azurekv:
                      description: >-
                        AzureKV configures this store to sync secrets using
                        Azure Key Vault provider
                      type: object
                      required:
                        - vaultUrl
                      properties:
                        authSecretRef:
                          description: >-
                            Auth configures how the operator authenticates with
                            Azure. Required for ServicePrincipal auth type.
                          type: object
                          properties:
                            clientId:
                              description: >-
                                The Azure clientId of the service principle used
                                for authentication.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            clientSecret:
                              description: >-
                                The Azure ClientSecret of the service principle
                                used for authentication.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        authType:
                          description: >-
                            Auth type defines how to authenticate to the
                            keyvault service. Valid values are: -
                            "ServicePrincipal" (default): Using a service
                            principal (tenantId, clientId, clientSecret) -
                            "ManagedIdentity": Using Managed Identity assigned
                            to the pod (see aad-pod-identity)
                          type: string
                          default: ServicePrincipal
                          enum:
                            - ServicePrincipal
                            - ManagedIdentity
                            - WorkloadIdentity
                        environmentType:
                          description: >-
                            EnvironmentType specifies the Azure cloud
                            environment endpoints to use for connecting and
                            authenticating with Azure. By default it points to
                            the public cloud AAD endpoint. The following
                            endpoints are available, also see here:
                            https://github.com/Azure/go-autorest/blob/main/autorest/azure/environments.go#L152
                            PublicCloud, USGovernmentCloud, ChinaCloud,
                            GermanCloud
                          type: string
                          default: PublicCloud
                          enum:
                            - PublicCloud
                            - USGovernmentCloud
                            - ChinaCloud
                            - GermanCloud
                        identityId:
                          description: >-
                            If multiple Managed Identity is assigned to the pod,
                            you can select the one to be used
                          type: string
                        serviceAccountRef:
                          description: >-
                            ServiceAccountRef specified the service account that
                            should be used when authenticating with
                            WorkloadIdentity.
                          type: object
                          required:
                            - name
                          properties:
                            audiences:
                              description: >-
                                Audience specifies the `aud` claim for the
                                service account token If the service account
                                uses a well-known annotation for e.g. IRSA or
                                GCP Workload Identity then this audiences will
                                be appended to the list
                              type: array
                              items:
                                type: string
                            name:
                              description: >-
                                The name of the ServiceAccount resource being
                                referred to.
                              type: string
                            namespace:
                              description: >-
                                Namespace of the resource being referred to.
                                Ignored if referent is not cluster-scoped.
                                cluster-scoped defaults to the namespace of the
                                referent.
                              type: string
                        tenantId:
                          description: >-
                            TenantID configures the Azure Tenant to send
                            requests to. Required for ServicePrincipal auth
                            type.
                          type: string
                        vaultUrl:
                          description: Vault Url from which the secrets to be fetched from.
                          type: string
                    doppler:
                      description: >-
                        Doppler configures this store to sync secrets using the
                        Doppler provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how the Operator authenticates with
                            the Doppler API
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              type: object
                              required:
                                - dopplerToken
                              properties:
                                dopplerToken:
                                  description: >-
                                    The DopplerToken is used for authentication.
                                    See
                                    https://docs.doppler.com/reference/api#authentication
                                    for auth token types. The Key attribute
                                    defaults to dopplerToken if not specified.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        config:
                          description: >-
                            Doppler config (required if not using a Service
                            Token)
                          type: string
                        format:
                          description: >-
                            Format enables the downloading of secrets as a file
                            (string)
                          type: string
                          enum:
                            - json
                            - dotnet-json
                            - env
                            - yaml
                            - docker
                        nameTransformer:
                          description: >-
                            Environment variable compatible name transforms that
                            change secret names to a different format
                          type: string
                          enum:
                            - upper-camel
                            - camel
                            - lower-snake
                            - tf-var
                            - dotnet-env
                        project:
                          description: >-
                            Doppler project (required if not using a Service
                            Token)
                          type: string
                    fake:
                      description: Fake configures a store with static key/value pairs
                      type: object
                      required:
                        - data
                      properties:
                        data:
                          type: array
                          items:
                            type: object
                            required:
                              - key
                            properties:
                              key:
                                type: string
                              value:
                                type: string
                              valueMap:
                                type: object
                                additionalProperties:
                                  type: string
                              version:
                                type: string
                    gcpsm:
                      description: >-
                        GCPSM configures this store to sync secrets using Google
                        Cloud Platform Secret Manager provider
                      type: object
                      properties:
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against GCP
                          type: object
                          properties:
                            secretRef:
                              type: object
                              properties:
                                secretAccessKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            workloadIdentity:
                              type: object
                              required:
                                - clusterLocation
                                - clusterName
                                - serviceAccountRef
                              properties:
                                clusterLocation:
                                  type: string
                                clusterName:
                                  type: string
                                clusterProjectID:
                                  type: string
                                serviceAccountRef:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        projectID:
                          description: ProjectID project where secret is located
                          type: string
                    gitlab:
                      description: >-
                        Gitlab configures this store to sync secrets using
                        Gitlab Variables provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with a GitLab instance.
                          type: object
                          required:
                            - SecretRef
                          properties:
                            SecretRef:
                              type: object
                              properties:
                                accessToken:
                                  description: AccessToken is used for authentication.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        environment:
                          description: >-
                            Environment environment_scope of gitlab CI/CD
                            variables (Please see
                            https://docs.gitlab.com/ee/ci/environments/#create-a-static-environment
                            on how to create environments)
                          type: string
                        groupIDs:
                          description: >-
                            GroupIDs specify, which gitlab groups to pull
                            secrets from. Group secrets are read from left to
                            right followed by the project variables.
                          type: array
                          items:
                            type: string
                        inheritFromGroups:
                          description: >-
                            InheritFromGroups specifies whether parent groups
                            should be discovered and checked for secrets.
                          type: boolean
                        projectID:
                          description: >-
                            ProjectID specifies a project where secrets are
                            located.
                          type: string
                        url:
                          description: >-
                            URL configures the GitLab instance URL. Defaults to
                            https://gitlab.com/.
                          type: string
                    ibm:
                      description: >-
                        IBM configures this store to sync secrets using IBM
                        Cloud provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the IBM secrets manager.
                          type: object
                          maxProperties: 1
                          minProperties: 1
                          properties:
                            containerAuth:
                              description: >-
                                IBM Container-based auth with IAM Trusted
                                Profile.
                              type: object
                              required:
                                - profile
                              properties:
                                iamEndpoint:
                                  type: string
                                profile:
                                  description: the IBM Trusted Profile
                                  type: string
                                tokenLocation:
                                  description: Location the token is mounted on the pod
                                  type: string
                            secretRef:
                              type: object
                              properties:
                                secretApiKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        serviceUrl:
                          description: >-
                            ServiceURL is the Endpoint URL that is specific to
                            the Secrets Manager service instance
                          type: string
                    keepersecurity:
                      description: >-
                        KeeperSecurity configures this store to sync secrets
                        using the KeeperSecurity provider
                      type: object
                      required:
                        - authRef
                        - folderID
                      properties:
                        authRef:
                          description: >-
                            A reference to a specific 'key' within a Secret
                            resource, In some instances, `key` is a required
                            field.
                          type: object
                          properties:
                            key:
                              description: >-
                                The key of the entry in the Secret resource's
                                `data` field to be used. Some instances of this
                                field may be defaulted, in others it may be
                                required.
                              type: string
                            name:
                              description: >-
                                The name of the Secret resource being referred
                                to.
                              type: string
                            namespace:
                              description: >-
                                Namespace of the resource being referred to.
                                Ignored if referent is not cluster-scoped.
                                cluster-scoped defaults to the namespace of the
                                referent.
                              type: string
                        folderID:
                          type: string
                    kubernetes:
                      description: >-
                        Kubernetes configures this store to sync secrets using a
                        Kubernetes cluster provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with a Kubernetes instance.
                          type: object
                          maxProperties: 1
                          minProperties: 1
                          properties:
                            cert:
                              description: >-
                                has both clientCert and clientKey as
                                secretKeySelector
                              type: object
                              properties:
                                clientCert:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                clientKey:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            serviceAccount:
                              description: >-
                                points to a service account that should be used
                                for authentication
                              type: object
                              required:
                                - name
                              properties:
                                audiences:
                                  description: >-
                                    Audience specifies the `aud` claim for the
                                    service account token If the service account
                                    uses a well-known annotation for e.g. IRSA
                                    or GCP Workload Identity then this audiences
                                    will be appended to the list
                                  type: array
                                  items:
                                    type: string
                                name:
                                  description: >-
                                    The name of the ServiceAccount resource
                                    being referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            token:
                              description: use static token to authenticate with
                              type: object
                              properties:
                                bearerToken:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        remoteNamespace:
                          description: Remote namespace to fetch the secrets from
                          type: string
                          default: default
                        server:
                          description: configures the Kubernetes server Address.
                          type: object
                          properties:
                            caBundle:
                              description: CABundle is a base64-encoded CA certificate
                              type: string
                              format: byte
                            caProvider:
                              description: >-
                                see:
                                https://external-secrets.io/v0.4.1/spec/#external-secrets.io/v1alpha1.CAProvider
                              type: object
                              required:
                                - name
                                - type
                              properties:
                                key:
                                  description: >-
                                    The key where the CA certificate can be
                                    found in the Secret or ConfigMap.
                                  type: string
                                name:
                                  description: >-
                                    The name of the object located at the
                                    provider type.
                                  type: string
                                namespace:
                                  description: >-
                                    The namespace the Provider type is in. Can
                                    only be defined when used in a
                                    ClusterSecretStore.
                                  type: string
                                type:
                                  description: >-
                                    The type of provider to use such as
                                    "Secret", or "ConfigMap".
                                  type: string
                                  enum:
                                    - Secret
                                    - ConfigMap
                            url:
                              description: configures the Kubernetes server Address.
                              type: string
                              default: kubernetes.default
                    onepassword:
                      description: >-
                        OnePassword configures this store to sync secrets using
                        the 1Password Cloud provider
                      type: object
                      required:
                        - auth
                        - connectHost
                        - vaults
                      properties:
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against OnePassword Connect Server
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              description: >-
                                OnePasswordAuthSecretRef holds secret references
                                for 1Password credentials.
                              type: object
                              required:
                                - connectTokenSecretRef
                              properties:
                                connectTokenSecretRef:
                                  description: >-
                                    The ConnectToken is used for authentication
                                    to a 1Password Connect Server.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        connectHost:
                          description: >-
                            ConnectHost defines the OnePassword Connect Server
                            to connect to
                          type: string
                        vaults:
                          description: >-
                            Vaults defines which OnePassword vaults to search in
                            which order
                          type: object
                          additionalProperties:
                            type: integer
                    oracle:
                      description: >-
                        Oracle configures this store to sync secrets using
                        Oracle Vault provider
                      type: object
                      required:
                        - region
                        - vault
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the Oracle Vault. If empty, use the instance
                            principal, otherwise the user credentials specified
                            in Auth.
                          type: object
                          required:
                            - secretRef
                            - tenancy
                            - user
                          properties:
                            secretRef:
                              description: SecretRef to pass through sensitive information.
                              type: object
                              required:
                                - fingerprint
                                - privatekey
                              properties:
                                fingerprint:
                                  description: >-
                                    Fingerprint is the fingerprint of the API
                                    private key.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                privatekey:
                                  description: >-
                                    PrivateKey is the user's API Signing Key in
                                    PEM format, used for authentication.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            tenancy:
                              description: >-
                                Tenancy is the tenancy OCID where user is
                                located.
                              type: string
                            user:
                              description: User is an access OCID specific to the account.
                              type: string
                        region:
                          description: Region is the region where vault is located.
                          type: string
                        vault:
                          description: >-
                            Vault is the vault's OCID of the specific vault
                            where secret is located.
                          type: string
                    scaleway:
                      description: Scaleway
                      type: object
                      required:
                        - accessKey
                        - projectId
                        - region
                        - secretKey
                      properties:
                        accessKey:
                          description: AccessKey is the non-secret part of the api key.
                          type: object
                          properties:
                            secretRef:
                              description: >-
                                SecretRef references a key in a secret that will
                                be used as value.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            value:
                              description: >-
                                Value can be specified directly to set a value
                                without using a secret.
                              type: string
                        apiUrl:
                          description: >-
                            APIURL is the url of the api to use. Defaults to
                            https://api.scaleway.com
                          type: string
                        projectId:
                          description: >-
                            ProjectID is the id of your project, which you can
                            find in the console:
                            https://console.scaleway.com/project/settings
                          type: string
                        region:
                          description: >-
                            Region where your secrets are located:
                            https://developers.scaleway.com/en/quickstart/#region-and-zone
                          type: string
                        secretKey:
                          description: SecretKey is the non-secret part of the api key.
                          type: object
                          properties:
                            secretRef:
                              description: >-
                                SecretRef references a key in a secret that will
                                be used as value.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            value:
                              description: >-
                                Value can be specified directly to set a value
                                without using a secret.
                              type: string
                    senhasegura:
                      description: >-
                        Senhasegura configures this store to sync secrets using
                        senhasegura provider
                      type: object
                      required:
                        - auth
                        - module
                        - url
                      properties:
                        auth:
                          description: >-
                            Auth defines parameters to authenticate in
                            senhasegura
                          type: object
                          required:
                            - clientId
                            - clientSecretSecretRef
                          properties:
                            clientId:
                              type: string
                            clientSecretSecretRef:
                              description: >-
                                A reference to a specific 'key' within a Secret
                                resource, In some instances, `key` is a required
                                field.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        ignoreSslCertificate:
                          description: >-
                            IgnoreSslCertificate defines if SSL certificate must
                            be ignored
                          type: boolean
                          default: false
                        module:
                          description: >-
                            Module defines which senhasegura module should be
                            used to get secrets
                          type: string
                        url:
                          description: URL of senhasegura
                          type: string
                    vault:
                      description: >-
                        Vault configures this store to sync secrets using Hashi
                        provider
                      type: object
                      required:
                        - auth
                        - server
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the Vault server.
                          type: object
                          properties:
                            appRole:
                              description: >-
                                AppRole authenticates with Vault using the App
                                Role auth mechanism, with the role and secret
                                stored in a Kubernetes Secret resource.
                              type: object
                              required:
                                - path
                                - roleId
                                - secretRef
                              properties:
                                path:
                                  description: >-
                                    Path where the App Role authentication
                                    backend is mounted in Vault, e.g: "approle"
                                  type: string
                                  default: approle
                                roleId:
                                  description: >-
                                    RoleID configured in the App Role
                                    authentication backend when setting up the
                                    authentication backend in Vault.
                                  type: string
                                secretRef:
                                  description: >-
                                    Reference to a key in a Secret that contains
                                    the App Role secret used to authenticate
                                    with Vault. The `key` field must be
                                    specified and denotes which entry within the
                                    Secret resource is used as the app role
                                    secret.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            cert:
                              description: >-
                                Cert authenticates with TLS Certificates by
                                passing client certificate, private key and ca
                                certificate Cert authentication method
                              type: object
                              properties:
                                clientCert:
                                  description: >-
                                    ClientCert is a certificate to authenticate
                                    using the Cert Vault authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                secretRef:
                                  description: >-
                                    SecretRef to a key in a Secret resource
                                    containing client private key to
                                    authenticate with Vault using the Cert
                                    authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            jwt:
                              description: >-
                                Jwt authenticates with Vault by passing role and
                                JWT token using the JWT/OIDC authentication
                                method
                              type: object
                              required:
                                - path
                              properties:
                                kubernetesServiceAccountToken:
                                  description: >-
                                    Optional ServiceAccountToken specifies the
                                    Kubernetes service account for which to
                                    request a token for with the `TokenRequest`
                                    API.
                                  type: object
                                  required:
                                    - serviceAccountRef
                                  properties:
                                    audiences:
                                      description: >-
                                        Optional audiences field that will be
                                        used to request a temporary Kubernetes
                                        service account token for the service
                                        account referenced by
                                        `serviceAccountRef`. Defaults to a
                                        single audience `vault` it not
                                        specified. Deprecated: use
                                        serviceAccountRef.Audiences instead
                                      type: array
                                      items:
                                        type: string
                                    expirationSeconds:
                                      description: >-
                                        Optional expiration time in seconds that
                                        will be used to request a temporary
                                        Kubernetes service account token for the
                                        service account referenced by
                                        `serviceAccountRef`. Deprecated: this
                                        will be removed in the future. Defaults
                                        to 10 minutes.
                                      type: integer
                                      format: int64
                                    serviceAccountRef:
                                      description: >-
                                        Service account field containing the
                                        name of a kubernetes ServiceAccount.
                                      type: object
                                      required:
                                        - name
                                      properties:
                                        audiences:
                                          description: >-
                                            Audience specifies the `aud` claim for
                                            the service account token If the service
                                            account uses a well-known annotation for
                                            e.g. IRSA or GCP Workload Identity then
                                            this audiences will be appended to the
                                            list
                                          type: array
                                          items:
                                            type: string
                                        name:
                                          description: >-
                                            The name of the ServiceAccount resource
                                            being referred to.
                                          type: string
                                        namespace:
                                          description: >-
                                            Namespace of the resource being referred
                                            to. Ignored if referent is not
                                            cluster-scoped. cluster-scoped defaults
                                            to the namespace of the referent.
                                          type: string
                                path:
                                  description: >-
                                    Path where the JWT authentication backend is
                                    mounted in Vault, e.g: "jwt"
                                  type: string
                                  default: jwt
                                role:
                                  description: >-
                                    Role is a JWT role to authenticate using the
                                    JWT/OIDC Vault authentication method
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional SecretRef that refers to a key in a
                                    Secret resource containing JWT token to
                                    authenticate with Vault using the JWT/OIDC
                                    authentication method.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            kubernetes:
                              description: >-
                                Kubernetes authenticates with Vault by passing
                                the ServiceAccount token stored in the named
                                Secret resource to the Vault server.
                              type: object
                              required:
                                - mountPath
                                - role
                              properties:
                                mountPath:
                                  description: >-
                                    Path where the Kubernetes authentication
                                    backend is mounted in Vault, e.g:
                                    "kubernetes"
                                  type: string
                                  default: kubernetes
                                role:
                                  description: >-
                                    A required field containing the Vault Role
                                    to assume. A Role binds a Kubernetes
                                    ServiceAccount with a set of Vault policies.
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional secret field containing a
                                    Kubernetes ServiceAccount JWT used for
                                    authenticating with Vault. If a name is
                                    specified without a key, `token` is the
                                    default. If one is not specified, the one
                                    bound to the controller will be used.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                serviceAccountRef:
                                  description: >-
                                    Optional service account field containing
                                    the name of a kubernetes ServiceAccount. If
                                    the service account is specified, the
                                    service account secret token JWT will be
                                    used for authenticating with Vault. If the
                                    service account selector is not supplied,
                                    the secretRef will be used instead.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            ldap:
                              description: >-
                                Ldap authenticates with Vault by passing
                                username/password pair using the LDAP
                                authentication method
                              type: object
                              required:
                                - path
                                - username
                              properties:
                                path:
                                  description: >-
                                    Path where the LDAP authentication backend
                                    is mounted in Vault, e.g: "ldap"
                                  type: string
                                  default: ldap
                                secretRef:
                                  description: >-
                                    SecretRef to a key in a Secret resource
                                    containing password for the LDAP user used
                                    to authenticate with Vault using the LDAP
                                    authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                username:
                                  description: >-
                                    Username is a LDAP user name used to
                                    authenticate using the LDAP Vault
                                    authentication method
                                  type: string
                            tokenSecretRef:
                              description: >-
                                TokenSecretRef authenticates with Vault by
                                presenting a token.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caBundle:
                          description: >-
                            PEM encoded CA bundle used to validate Vault server
                            certificate. Only used if the Server URL is using
                            HTTPS protocol. This parameter is ignored for plain
                            HTTP protocol connection. If not set the system root
                            certificates are used to validate the TLS
                            connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Vault server certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key where the CA certificate can be found in
                                the Secret or ConfigMap.
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: >-
                                The namespace the Provider type is in. Can only
                                be defined when used in a ClusterSecretStore.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                        forwardInconsistent:
                          description: >-
                            ForwardInconsistent tells Vault to forward
                            read-after-write requests to the Vault leader
                            instead of simply retrying within a loop. This can
                            increase performance if the option is enabled
                            serverside.
                            https://www.vaultproject.io/docs/configuration/replication#allow_forwarding_via_header
                          type: boolean
                        namespace:
                          description: >-
                            Name of the vault namespace. Namespaces is a set of
                            features within Vault Enterprise that allows Vault
                            environments to support Secure Multi-tenancy. e.g:
                            "ns1". More about namespaces can be found here
                            https://www.vaultproject.io/docs/enterprise/namespaces
                          type: string
                        path:
                          description: >-
                            Path is the mount path of the Vault KV backend
                            endpoint, e.g: "secret". The v2 KV secret engine
                            version specific "/data" path suffix for fetching
                            secrets from Vault is optional and will be appended
                            if not present in specified path.
                          type: string
                        readYourWrites:
                          description: >-
                            ReadYourWrites ensures isolated read-after-write
                            semantics by providing discovered cluster
                            replication states in each request. More information
                            about eventual consistency in Vault can be found
                            here
                            https://www.vaultproject.io/docs/enterprise/consistency
                          type: boolean
                        server:
                          description: >-
                            Server is the connection address for the Vault
                            server, e.g: "https://vault.example.com:8200".
                          type: string
                        version:
                          description: >-
                            Version is the Vault KV secret engine version. This
                            can be either "v1" or "v2". Version defaults to
                            "v2".
                          type: string
                          default: v2
                          enum:
                            - v1
                            - v2
                    webhook:
                      description: >-
                        Webhook configures this store to sync secrets using a
                        generic templated webhook
                      type: object
                      required:
                        - result
                        - url
                      properties:
                        body:
                          description: Body
                          type: string
                        caBundle:
                          description: >-
                            PEM encoded CA bundle used to validate webhook
                            server certificate. Only used if the Server URL is
                            using HTTPS protocol. This parameter is ignored for
                            plain HTTP protocol connection. If not set the
                            system root certificates are used to validate the
                            TLS connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            webhook server certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key the value inside of the provider type to
                                use, only used with "Secret" type
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: The namespace the Provider type is in.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                        headers:
                          description: Headers
                          type: object
                          additionalProperties:
                            type: string
                        method:
                          description: Webhook Method
                          type: string
                        result:
                          description: Result formatting
                          type: object
                          properties:
                            jsonPath:
                              description: Json path of return value
                              type: string
                        secrets:
                          description: >-
                            Secrets to fill in templates These secrets will be
                            passed to the templating function as key value pairs
                            under the given name
                          type: array
                          items:
                            type: object
                            required:
                              - name
                              - secretRef
                            properties:
                              name:
                                description: Name of this secret in templates
                                type: string
                              secretRef:
                                description: Secret ref to fill in credentials
                                type: object
                                properties:
                                  key:
                                    description: >-
                                      The key of the entry in the Secret
                                      resource's `data` field to be used. Some
                                      instances of this field may be defaulted,
                                      in others it may be required.
                                    type: string
                                  name:
                                    description: >-
                                      The name of the Secret resource being
                                      referred to.
                                    type: string
                                  namespace:
                                    description: >-
                                      Namespace of the resource being referred
                                      to. Ignored if referent is not
                                      cluster-scoped. cluster-scoped defaults to
                                      the namespace of the referent.
                                    type: string
                        timeout:
                          description: Timeout
                          type: string
                        url:
                          description: Webhook url to call
                          type: string
                    yandexcertificatemanager:
                      description: >-
                        YandexCertificateManager configures this store to sync
                        secrets using Yandex Certificate Manager provider
                      type: object
                      required:
                        - auth
                      properties:
                        apiEndpoint:
                          description: >-
                            Yandex.Cloud API endpoint (e.g.
                            'api.cloud.yandex.net:443')
                          type: string
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against Yandex Certificate Manager
                          type: object
                          properties:
                            authorizedKeySecretRef:
                              description: The authorized key used for authentication
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Yandex.Cloud server certificate.
                          type: object
                          properties:
                            certSecretRef:
                              description: >-
                                A reference to a specific 'key' within a Secret
                                resource, In some instances, `key` is a required
                                field.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                    yandexlockbox:
                      description: >-
                        YandexLockbox configures this store to sync secrets
                        using Yandex Lockbox provider
                      type: object
                      required:
                        - auth
                      properties:
                        apiEndpoint:
                          description: >-
                            Yandex.Cloud API endpoint (e.g.
                            'api.cloud.yandex.net:443')
                          type: string
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against Yandex Lockbox
                          type: object
                          properties:
                            authorizedKeySecretRef:
                              description: The authorized key used for authentication
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Yandex.Cloud server certificate.
                          type: object
                          properties:
                            certSecretRef:
                              description: >-
                                A reference to a specific 'key' within a Secret
                                resource, In some instances, `key` is a required
                                field.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                refreshInterval:
                  description: >-
                    Used to configure store refresh interval in seconds. Empty
                    or 0 will default to the controller config.
                  type: integer
                retrySettings:
                  description: Used to configure http retries if failed
                  type: object
                  properties:
                    maxRetries:
                      type: integer
                      format: int32
                    retryInterval:
                      type: string
            status:
              description: SecretStoreStatus defines the observed state of the SecretStore.
              type: object
              properties:
                capabilities:
                  description: >-
                    SecretStoreCapabilities defines the possible operations a
                    SecretStore can do.
                  type: string
                conditions:
                  type: array
                  items:
                    type: object
                    required:
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        type: string
                        format: date-time
                      message:
                        type: string
                      reason:
                        type: string
                      status:
                        type: string
                      type:
                        type: string
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: AGE
          type: date
          jsonPath: .metadata.creationTimestamp
        - name: Status
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].reason
        - name: Capabilities
          type: string
          jsonPath: .status.capabilities
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          namespace: external-secrets
          name: external-secrets-external-secrets-webhook
          path: /convert
          port: 443
        caBundle: >-
          
      conversionReviewVersions:
        - v1
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: secretstores.external-secrets.io
  uid: 688cc821-ae79-4333-ae5d-28bf97ccecdb
  resourceVersion: '2044645'
  generation: 2
  creationTimestamp: '2023-03-29T16:16:10Z'
  labels:
    app.kubernetes.io/managed-by: Helm
    helm.toolkit.fluxcd.io/name: external-secrets
    helm.toolkit.fluxcd.io/namespace: flux-system
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
    meta.helm.sh/release-name: external-secrets-external-secrets
    meta.helm.sh/release-namespace: external-secrets
  managedFields:
    - manager: helm-controller
      operation: Update
      apiVersion: apiextensions.k8s.io/v1
      time: '2023-03-29T16:16:10Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:metadata:
          f:annotations:
            .: {}
            f:controller-gen.kubebuilder.io/version: {}
            f:meta.helm.sh/release-name: {}
            f:meta.helm.sh/release-namespace: {}
          f:labels:
            .: {}
            f:app.kubernetes.io/managed-by: {}
            f:helm.toolkit.fluxcd.io/name: {}
            f:helm.toolkit.fluxcd.io/namespace: {}
        f:spec:
          f:conversion:
            .: {}
            f:strategy: {}
            f:webhook:
              .: {}
              f:clientConfig:
                .: {}
                f:service:
                  .: {}
                  f:name: {}
                  f:namespace: {}
                  f:path: {}
                  f:port: {}
              f:conversionReviewVersions: {}
          f:group: {}
          f:names:
            f:categories: {}
            f:kind: {}
            f:listKind: {}
            f:plural: {}
            f:shortNames: {}
            f:singular: {}
          f:scope: {}
          f:versions: {}
    - manager: k3s
      operation: Update
      apiVersion: apiextensions.k8s.io/v1
      time: '2023-03-29T16:16:11Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:status:
          f:acceptedNames:
            f:categories: {}
            f:kind: {}
            f:listKind: {}
            f:plural: {}
            f:shortNames: {}
            f:singular: {}
          f:conditions:
            k:{"type":"Established"}:
              .: {}
              f:lastTransitionTime: {}
              f:message: {}
              f:reason: {}
              f:status: {}
              f:type: {}
            k:{"type":"NamesAccepted"}:
              .: {}
              f:lastTransitionTime: {}
              f:message: {}
              f:reason: {}
              f:status: {}
              f:type: {}
      subresource: status
    - manager: external-secrets
      operation: Update
      apiVersion: apiextensions.k8s.io/v1
      time: '2023-03-29T16:16:14Z'
      fieldsType: FieldsV1
      fieldsV1:
        f:spec:
          f:conversion:
            f:webhook:
              f:clientConfig:
                f:caBundle: {}
  selfLink: >-
    /apis/apiextensions.k8s.io/v1/customresourcedefinitions/secretstores.external-secrets.io
status:
  conditions:
    - type: NamesAccepted
      status: 'True'
      lastTransitionTime: '2023-03-29T16:16:11Z'
      reason: NoConflicts
      message: no conflicts found
    - type: Established
      status: 'True'
      lastTransitionTime: '2023-03-29T16:16:11Z'
      reason: InitialNamesAccepted
      message: the initial names have been accepted
  acceptedNames:
    plural: secretstores
    singular: secretstore
    shortNames:
      - ss
    kind: SecretStore
    listKind: SecretStoreList
    categories:
      - externalsecrets
  storedVersions:
    - v1beta1
spec:
  group: external-secrets.io
  names:
    plural: secretstores
    singular: secretstore
    shortNames:
      - ss
    kind: SecretStore
    listKind: SecretStoreList
    categories:
      - externalsecrets
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: false
      deprecated: true
      schema:
        openAPIV3Schema:
          description: >-
            SecretStore represents a secure external location for storing
            secrets, which can be referenced as part of `storeRef` fields.
          type: object
          properties:
            apiVersion:
              description: >-
                APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the
                latest internal value, and may reject unrecognized values. More
                info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: >-
                Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase.
                More info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: SecretStoreSpec defines the desired state of SecretStore.
              type: object
              required:
                - provider
              properties:
                controller:
                  description: >-
                    Used to select the correct KES controller (think:
                    ingress.ingressClassName) The KES controller is instantiated
                    with a specific controller name and filters ES based on this
                    property
                  type: string
                provider:
                  description: Used to configure the provider. Only one provider may be set
                  type: object
                  maxProperties: 1
                  minProperties: 1
                  properties:
                    akeyless:
                      description: >-
                        Akeyless configures this store to sync secrets using
                        Akeyless Vault provider
                      type: object
                      required:
                        - akeylessGWApiURL
                        - authSecretRef
                      properties:
                        akeylessGWApiURL:
                          description: >-
                            Akeyless GW API Url from which the secrets to be
                            fetched from.
                          type: string
                        authSecretRef:
                          description: >-
                            Auth configures how the operator authenticates with
                            Akeyless.
                          type: object
                          properties:
                            kubernetesAuth:
                              description: >-
                                Kubernetes authenticates with Akeyless by
                                passing the ServiceAccount token stored in the
                                named Secret resource.
                              type: object
                              required:
                                - accessID
                                - k8sConfName
                              properties:
                                accessID:
                                  description: >-
                                    the Akeyless Kubernetes auth-method
                                    access-id
                                  type: string
                                k8sConfName:
                                  description: >-
                                    Kubernetes-auth configuration name in
                                    Akeyless-Gateway
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional secret field containing a
                                    Kubernetes ServiceAccount JWT used for
                                    authenticating with Akeyless. If a name is
                                    specified without a key, `token` is the
                                    default. If one is not specified, the one
                                    bound to the controller will be used.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                serviceAccountRef:
                                  description: >-
                                    Optional service account field containing
                                    the name of a kubernetes ServiceAccount. If
                                    the service account is specified, the
                                    service account secret token JWT will be
                                    used for authenticating with Akeyless. If
                                    the service account selector is not
                                    supplied, the secretRef will be used
                                    instead.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            secretRef:
                              description: >-
                                Reference to a Secret that contains the details
                                to authenticate with Akeyless.
                              type: object
                              properties:
                                accessID:
                                  description: >-
                                    The SecretAccessID is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessType:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessTypeParam:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        caBundle:
                          description: >-
                            PEM/base64 encoded CA bundle used to validate
                            Akeyless Gateway certificate. Only used if the
                            AkeylessGWApiURL URL is using HTTPS protocol. If not
                            set the system root certificates are used to
                            validate the TLS connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Akeyless Gateway certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key the value inside of the provider type to
                                use, only used with "Secret" type
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: The namespace the Provider type is in.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                    alibaba:
                      description: >-
                        Alibaba configures this store to sync secrets using
                        Alibaba Cloud provider
                      type: object
                      required:
                        - auth
                        - regionID
                      properties:
                        auth:
                          description: AlibabaAuth contains a secretRef for credentials.
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              description: >-
                                AlibabaAuthSecretRef holds secret references for
                                Alibaba credentials.
                              type: object
                              required:
                                - accessKeyIDSecretRef
                                - accessKeySecretSecretRef
                              properties:
                                accessKeyIDSecretRef:
                                  description: The AccessKeyID is used for authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessKeySecretSecretRef:
                                  description: >-
                                    The AccessKeySecret is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        endpoint:
                          type: string
                        regionID:
                          description: Alibaba Region to be used for the provider
                          type: string
                    aws:
                      description: >-
                        AWS configures this store to sync secrets using AWS
                        Secret Manager provider
                      type: object
                      required:
                        - region
                        - service
                      properties:
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against AWS if not set aws sdk will
                            infer credentials from your environment see:
                            https://docs.aws.amazon.com/sdk-for-go/v1/developer-guide/configuring-sdk.html#specifying-credentials
                          type: object
                          properties:
                            jwt:
                              description: >-
                                Authenticate against AWS using service account
                                tokens.
                              type: object
                              properties:
                                serviceAccountRef:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            secretRef:
                              description: >-
                                AWSAuthSecretRef holds secret references for AWS
                                credentials both AccessKeyID and SecretAccessKey
                                must be defined in order to properly
                                authenticate.
                              type: object
                              properties:
                                accessKeyIDSecretRef:
                                  description: The AccessKeyID is used for authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                secretAccessKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        region:
                          description: AWS Region to be used for the provider
                          type: string
                        role:
                          description: >-
                            Role is a Role ARN which the SecretManager provider
                            will assume
                          type: string
                        service:
                          description: >-
                            Service defines which service should be used to
                            fetch the secrets
                          type: string
                          enum:
                            - SecretsManager
                            - ParameterStore
                    azurekv:
                      description: >-
                        AzureKV configures this store to sync secrets using
                        Azure Key Vault provider
                      type: object
                      required:
                        - vaultUrl
                      properties:
                        authSecretRef:
                          description: >-
                            Auth configures how the operator authenticates with
                            Azure. Required for ServicePrincipal auth type.
                          type: object
                          properties:
                            clientId:
                              description: >-
                                The Azure clientId of the service principle used
                                for authentication.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            clientSecret:
                              description: >-
                                The Azure ClientSecret of the service principle
                                used for authentication.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        authType:
                          description: >-
                            Auth type defines how to authenticate to the
                            keyvault service. Valid values are: -
                            "ServicePrincipal" (default): Using a service
                            principal (tenantId, clientId, clientSecret) -
                            "ManagedIdentity": Using Managed Identity assigned
                            to the pod (see aad-pod-identity)
                          type: string
                          default: ServicePrincipal
                          enum:
                            - ServicePrincipal
                            - ManagedIdentity
                            - WorkloadIdentity
                        identityId:
                          description: >-
                            If multiple Managed Identity is assigned to the pod,
                            you can select the one to be used
                          type: string
                        serviceAccountRef:
                          description: >-
                            ServiceAccountRef specified the service account that
                            should be used when authenticating with
                            WorkloadIdentity.
                          type: object
                          required:
                            - name
                          properties:
                            audiences:
                              description: >-
                                Audience specifies the `aud` claim for the
                                service account token If the service account
                                uses a well-known annotation for e.g. IRSA or
                                GCP Workload Identity then this audiences will
                                be appended to the list
                              type: array
                              items:
                                type: string
                            name:
                              description: >-
                                The name of the ServiceAccount resource being
                                referred to.
                              type: string
                            namespace:
                              description: >-
                                Namespace of the resource being referred to.
                                Ignored if referent is not cluster-scoped.
                                cluster-scoped defaults to the namespace of the
                                referent.
                              type: string
                        tenantId:
                          description: >-
                            TenantID configures the Azure Tenant to send
                            requests to. Required for ServicePrincipal auth
                            type.
                          type: string
                        vaultUrl:
                          description: Vault Url from which the secrets to be fetched from.
                          type: string
                    fake:
                      description: Fake configures a store with static key/value pairs
                      type: object
                      required:
                        - data
                      properties:
                        data:
                          type: array
                          items:
                            type: object
                            required:
                              - key
                            properties:
                              key:
                                type: string
                              value:
                                type: string
                              valueMap:
                                type: object
                                additionalProperties:
                                  type: string
                              version:
                                type: string
                    gcpsm:
                      description: >-
                        GCPSM configures this store to sync secrets using Google
                        Cloud Platform Secret Manager provider
                      type: object
                      properties:
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against GCP
                          type: object
                          properties:
                            secretRef:
                              type: object
                              properties:
                                secretAccessKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            workloadIdentity:
                              type: object
                              required:
                                - clusterLocation
                                - clusterName
                                - serviceAccountRef
                              properties:
                                clusterLocation:
                                  type: string
                                clusterName:
                                  type: string
                                clusterProjectID:
                                  type: string
                                serviceAccountRef:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        projectID:
                          description: ProjectID project where secret is located
                          type: string
                    gitlab:
                      description: >-
                        Gitlab configures this store to sync secrets using
                        Gitlab Variables provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with a GitLab instance.
                          type: object
                          required:
                            - SecretRef
                          properties:
                            SecretRef:
                              type: object
                              properties:
                                accessToken:
                                  description: AccessToken is used for authentication.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        projectID:
                          description: >-
                            ProjectID specifies a project where secrets are
                            located.
                          type: string
                        url:
                          description: >-
                            URL configures the GitLab instance URL. Defaults to
                            https://gitlab.com/.
                          type: string
                    ibm:
                      description: >-
                        IBM configures this store to sync secrets using IBM
                        Cloud provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the IBM secrets manager.
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              type: object
                              properties:
                                secretApiKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        serviceUrl:
                          description: >-
                            ServiceURL is the Endpoint URL that is specific to
                            the Secrets Manager service instance
                          type: string
                    kubernetes:
                      description: >-
                        Kubernetes configures this store to sync secrets using a
                        Kubernetes cluster provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with a Kubernetes instance.
                          type: object
                          maxProperties: 1
                          minProperties: 1
                          properties:
                            cert:
                              description: >-
                                has both clientCert and clientKey as
                                secretKeySelector
                              type: object
                              properties:
                                clientCert:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                clientKey:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            serviceAccount:
                              description: >-
                                points to a service account that should be used
                                for authentication
                              type: object
                              properties:
                                serviceAccount:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            token:
                              description: use static token to authenticate with
                              type: object
                              properties:
                                bearerToken:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        remoteNamespace:
                          description: Remote namespace to fetch the secrets from
                          type: string
                          default: default
                        server:
                          description: configures the Kubernetes server Address.
                          type: object
                          properties:
                            caBundle:
                              description: CABundle is a base64-encoded CA certificate
                              type: string
                              format: byte
                            caProvider:
                              description: >-
                                see:
                                https://external-secrets.io/v0.4.1/spec/#external-secrets.io/v1alpha1.CAProvider
                              type: object
                              required:
                                - name
                                - type
                              properties:
                                key:
                                  description: >-
                                    The key the value inside of the provider
                                    type to use, only used with "Secret" type
                                  type: string
                                name:
                                  description: >-
                                    The name of the object located at the
                                    provider type.
                                  type: string
                                namespace:
                                  description: The namespace the Provider type is in.
                                  type: string
                                type:
                                  description: >-
                                    The type of provider to use such as
                                    "Secret", or "ConfigMap".
                                  type: string
                                  enum:
                                    - Secret
                                    - ConfigMap
                            url:
                              description: configures the Kubernetes server Address.
                              type: string
                              default: kubernetes.default
                    oracle:
                      description: >-
                        Oracle configures this store to sync secrets using
                        Oracle Vault provider
                      type: object
                      required:
                        - region
                        - vault
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the Oracle Vault. If empty, use the instance
                            principal, otherwise the user credentials specified
                            in Auth.
                          type: object
                          required:
                            - secretRef
                            - tenancy
                            - user
                          properties:
                            secretRef:
                              description: SecretRef to pass through sensitive information.
                              type: object
                              required:
                                - fingerprint
                                - privatekey
                              properties:
                                fingerprint:
                                  description: >-
                                    Fingerprint is the fingerprint of the API
                                    private key.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                privatekey:
                                  description: >-
                                    PrivateKey is the user's API Signing Key in
                                    PEM format, used for authentication.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            tenancy:
                              description: >-
                                Tenancy is the tenancy OCID where user is
                                located.
                              type: string
                            user:
                              description: User is an access OCID specific to the account.
                              type: string
                        region:
                          description: Region is the region where vault is located.
                          type: string
                        vault:
                          description: >-
                            Vault is the vault's OCID of the specific vault
                            where secret is located.
                          type: string
                    vault:
                      description: >-
                        Vault configures this store to sync secrets using Hashi
                        provider
                      type: object
                      required:
                        - auth
                        - server
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the Vault server.
                          type: object
                          properties:
                            appRole:
                              description: >-
                                AppRole authenticates with Vault using the App
                                Role auth mechanism, with the role and secret
                                stored in a Kubernetes Secret resource.
                              type: object
                              required:
                                - path
                                - roleId
                                - secretRef
                              properties:
                                path:
                                  description: >-
                                    Path where the App Role authentication
                                    backend is mounted in Vault, e.g: "approle"
                                  type: string
                                  default: approle
                                roleId:
                                  description: >-
                                    RoleID configured in the App Role
                                    authentication backend when setting up the
                                    authentication backend in Vault.
                                  type: string
                                secretRef:
                                  description: >-
                                    Reference to a key in a Secret that contains
                                    the App Role secret used to authenticate
                                    with Vault. The `key` field must be
                                    specified and denotes which entry within the
                                    Secret resource is used as the app role
                                    secret.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            cert:
                              description: >-
                                Cert authenticates with TLS Certificates by
                                passing client certificate, private key and ca
                                certificate Cert authentication method
                              type: object
                              properties:
                                clientCert:
                                  description: >-
                                    ClientCert is a certificate to authenticate
                                    using the Cert Vault authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                secretRef:
                                  description: >-
                                    SecretRef to a key in a Secret resource
                                    containing client private key to
                                    authenticate with Vault using the Cert
                                    authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            jwt:
                              description: >-
                                Jwt authenticates with Vault by passing role and
                                JWT token using the JWT/OIDC authentication
                                method
                              type: object
                              required:
                                - path
                              properties:
                                kubernetesServiceAccountToken:
                                  description: >-
                                    Optional ServiceAccountToken specifies the
                                    Kubernetes service account for which to
                                    request a token for with the `TokenRequest`
                                    API.
                                  type: object
                                  required:
                                    - serviceAccountRef
                                  properties:
                                    audiences:
                                      description: >-
                                        Optional audiences field that will be
                                        used to request a temporary Kubernetes
                                        service account token for the service
                                        account referenced by
                                        `serviceAccountRef`. Defaults to a
                                        single audience `vault` it not
                                        specified.
                                      type: array
                                      items:
                                        type: string
                                    expirationSeconds:
                                      description: >-
                                        Optional expiration time in seconds that
                                        will be used to request a temporary
                                        Kubernetes service account token for the
                                        service account referenced by
                                        `serviceAccountRef`. Defaults to 10
                                        minutes.
                                      type: integer
                                      format: int64
                                    serviceAccountRef:
                                      description: >-
                                        Service account field containing the
                                        name of a kubernetes ServiceAccount.
                                      type: object
                                      required:
                                        - name
                                      properties:
                                        audiences:
                                          description: >-
                                            Audience specifies the `aud` claim for
                                            the service account token If the service
                                            account uses a well-known annotation for
                                            e.g. IRSA or GCP Workload Identity then
                                            this audiences will be appended to the
                                            list
                                          type: array
                                          items:
                                            type: string
                                        name:
                                          description: >-
                                            The name of the ServiceAccount resource
                                            being referred to.
                                          type: string
                                        namespace:
                                          description: >-
                                            Namespace of the resource being referred
                                            to. Ignored if referent is not
                                            cluster-scoped. cluster-scoped defaults
                                            to the namespace of the referent.
                                          type: string
                                path:
                                  description: >-
                                    Path where the JWT authentication backend is
                                    mounted in Vault, e.g: "jwt"
                                  type: string
                                  default: jwt
                                role:
                                  description: >-
                                    Role is a JWT role to authenticate using the
                                    JWT/OIDC Vault authentication method
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional SecretRef that refers to a key in a
                                    Secret resource containing JWT token to
                                    authenticate with Vault using the JWT/OIDC
                                    authentication method.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            kubernetes:
                              description: >-
                                Kubernetes authenticates with Vault by passing
                                the ServiceAccount token stored in the named
                                Secret resource to the Vault server.
                              type: object
                              required:
                                - mountPath
                                - role
                              properties:
                                mountPath:
                                  description: >-
                                    Path where the Kubernetes authentication
                                    backend is mounted in Vault, e.g:
                                    "kubernetes"
                                  type: string
                                  default: kubernetes
                                role:
                                  description: >-
                                    A required field containing the Vault Role
                                    to assume. A Role binds a Kubernetes
                                    ServiceAccount with a set of Vault policies.
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional secret field containing a
                                    Kubernetes ServiceAccount JWT used for
                                    authenticating with Vault. If a name is
                                    specified without a key, `token` is the
                                    default. If one is not specified, the one
                                    bound to the controller will be used.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                serviceAccountRef:
                                  description: >-
                                    Optional service account field containing
                                    the name of a kubernetes ServiceAccount. If
                                    the service account is specified, the
                                    service account secret token JWT will be
                                    used for authenticating with Vault. If the
                                    service account selector is not supplied,
                                    the secretRef will be used instead.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            ldap:
                              description: >-
                                Ldap authenticates with Vault by passing
                                username/password pair using the LDAP
                                authentication method
                              type: object
                              required:
                                - path
                                - username
                              properties:
                                path:
                                  description: >-
                                    Path where the LDAP authentication backend
                                    is mounted in Vault, e.g: "ldap"
                                  type: string
                                  default: ldap
                                secretRef:
                                  description: >-
                                    SecretRef to a key in a Secret resource
                                    containing password for the LDAP user used
                                    to authenticate with Vault using the LDAP
                                    authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                username:
                                  description: >-
                                    Username is a LDAP user name used to
                                    authenticate using the LDAP Vault
                                    authentication method
                                  type: string
                            tokenSecretRef:
                              description: >-
                                TokenSecretRef authenticates with Vault by
                                presenting a token.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caBundle:
                          description: >-
                            PEM encoded CA bundle used to validate Vault server
                            certificate. Only used if the Server URL is using
                            HTTPS protocol. This parameter is ignored for plain
                            HTTP protocol connection. If not set the system root
                            certificates are used to validate the TLS
                            connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Vault server certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key the value inside of the provider type to
                                use, only used with "Secret" type
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: The namespace the Provider type is in.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                        forwardInconsistent:
                          description: >-
                            ForwardInconsistent tells Vault to forward
                            read-after-write requests to the Vault leader
                            instead of simply retrying within a loop. This can
                            increase performance if the option is enabled
                            serverside.
                            https://www.vaultproject.io/docs/configuration/replication#allow_forwarding_via_header
                          type: boolean
                        namespace:
                          description: >-
                            Name of the vault namespace. Namespaces is a set of
                            features within Vault Enterprise that allows Vault
                            environments to support Secure Multi-tenancy. e.g:
                            "ns1". More about namespaces can be found here
                            https://www.vaultproject.io/docs/enterprise/namespaces
                          type: string
                        path:
                          description: >-
                            Path is the mount path of the Vault KV backend
                            endpoint, e.g: "secret". The v2 KV secret engine
                            version specific "/data" path suffix for fetching
                            secrets from Vault is optional and will be appended
                            if not present in specified path.
                          type: string
                        readYourWrites:
                          description: >-
                            ReadYourWrites ensures isolated read-after-write
                            semantics by providing discovered cluster
                            replication states in each request. More information
                            about eventual consistency in Vault can be found
                            here
                            https://www.vaultproject.io/docs/enterprise/consistency
                          type: boolean
                        server:
                          description: >-
                            Server is the connection address for the Vault
                            server, e.g: "https://vault.example.com:8200".
                          type: string
                        version:
                          description: >-
                            Version is the Vault KV secret engine version. This
                            can be either "v1" or "v2". Version defaults to
                            "v2".
                          type: string
                          default: v2
                          enum:
                            - v1
                            - v2
                    webhook:
                      description: >-
                        Webhook configures this store to sync secrets using a
                        generic templated webhook
                      type: object
                      required:
                        - result
                        - url
                      properties:
                        body:
                          description: Body
                          type: string
                        caBundle:
                          description: >-
                            PEM encoded CA bundle used to validate webhook
                            server certificate. Only used if the Server URL is
                            using HTTPS protocol. This parameter is ignored for
                            plain HTTP protocol connection. If not set the
                            system root certificates are used to validate the
                            TLS connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            webhook server certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key the value inside of the provider type to
                                use, only used with "Secret" type
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: The namespace the Provider type is in.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                        headers:
                          description: Headers
                          type: object
                          additionalProperties:
                            type: string
                        method:
                          description: Webhook Method
                          type: string
                        result:
                          description: Result formatting
                          type: object
                          properties:
                            jsonPath:
                              description: Json path of return value
                              type: string
                        secrets:
                          description: >-
                            Secrets to fill in templates These secrets will be
                            passed to the templating function as key value pairs
                            under the given name
                          type: array
                          items:
                            type: object
                            required:
                              - name
                              - secretRef
                            properties:
                              name:
                                description: Name of this secret in templates
                                type: string
                              secretRef:
                                description: Secret ref to fill in credentials
                                type: object
                                properties:
                                  key:
                                    description: >-
                                      The key of the entry in the Secret
                                      resource's `data` field to be used. Some
                                      instances of this field may be defaulted,
                                      in others it may be required.
                                    type: string
                                  name:
                                    description: >-
                                      The name of the Secret resource being
                                      referred to.
                                    type: string
                                  namespace:
                                    description: >-
                                      Namespace of the resource being referred
                                      to. Ignored if referent is not
                                      cluster-scoped. cluster-scoped defaults to
                                      the namespace of the referent.
                                    type: string
                        timeout:
                          description: Timeout
                          type: string
                        url:
                          description: Webhook url to call
                          type: string
                    yandexlockbox:
                      description: >-
                        YandexLockbox configures this store to sync secrets
                        using Yandex Lockbox provider
                      type: object
                      required:
                        - auth
                      properties:
                        apiEndpoint:
                          description: >-
                            Yandex.Cloud API endpoint (e.g.
                            'api.cloud.yandex.net:443')
                          type: string
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against Yandex Lockbox
                          type: object
                          properties:
                            authorizedKeySecretRef:
                              description: The authorized key used for authentication
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Yandex.Cloud server certificate.
                          type: object
                          properties:
                            certSecretRef:
                              description: >-
                                A reference to a specific 'key' within a Secret
                                resource, In some instances, `key` is a required
                                field.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                retrySettings:
                  description: Used to configure http retries if failed
                  type: object
                  properties:
                    maxRetries:
                      type: integer
                      format: int32
                    retryInterval:
                      type: string
            status:
              description: SecretStoreStatus defines the observed state of the SecretStore.
              type: object
              properties:
                conditions:
                  type: array
                  items:
                    type: object
                    required:
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        type: string
                        format: date-time
                      message:
                        type: string
                      reason:
                        type: string
                      status:
                        type: string
                      type:
                        type: string
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: AGE
          type: date
          jsonPath: .metadata.creationTimestamp
        - name: Status
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].reason
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          description: >-
            SecretStore represents a secure external location for storing
            secrets, which can be referenced as part of `storeRef` fields.
          type: object
          properties:
            apiVersion:
              description: >-
                APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the
                latest internal value, and may reject unrecognized values. More
                info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: >-
                Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase.
                More info:
                https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: SecretStoreSpec defines the desired state of SecretStore.
              type: object
              required:
                - provider
              properties:
                conditions:
                  description: >-
                    Used to constraint a ClusterSecretStore to specific
                    namespaces. Relevant only to ClusterSecretStore
                  type: array
                  items:
                    description: >-
                      ClusterSecretStoreCondition describes a condition by which
                      to choose namespaces to process ExternalSecrets in for a
                      ClusterSecretStore instance.
                    type: object
                    properties:
                      namespaceSelector:
                        description: Choose namespace using a labelSelector
                        type: object
                        properties:
                          matchExpressions:
                            description: >-
                              matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            type: array
                            items:
                              description: >-
                                A label selector requirement is a selector that
                                contains values, a key, and an operator that
                                relates the key and values.
                              type: object
                              required:
                                - key
                                - operator
                              properties:
                                key:
                                  description: >-
                                    key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: >-
                                    operator represents a key's relationship to
                                    a set of values. Valid operators are In,
                                    NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: >-
                                    values is an array of string values. If the
                                    operator is In or NotIn, the values array
                                    must be non-empty. If the operator is Exists
                                    or DoesNotExist, the values array must be
                                    empty. This array is replaced during a
                                    strategic merge patch.
                                  type: array
                                  items:
                                    type: string
                          matchLabels:
                            description: >-
                              matchLabels is a map of {key,value} pairs. A
                              single {key,value} in the matchLabels map is
                              equivalent to an element of matchExpressions,
                              whose key field is "key", the operator is "In",
                              and the values array contains only "value". The
                              requirements are ANDed.
                            type: object
                            additionalProperties:
                              type: string
                        x-kubernetes-map-type: atomic
                      namespaces:
                        description: Choose namespaces by name
                        type: array
                        items:
                          type: string
                controller:
                  description: >-
                    Used to select the correct KES controller (think:
                    ingress.ingressClassName) The KES controller is instantiated
                    with a specific controller name and filters ES based on this
                    property
                  type: string
                provider:
                  description: Used to configure the provider. Only one provider may be set
                  type: object
                  maxProperties: 1
                  minProperties: 1
                  properties:
                    akeyless:
                      description: >-
                        Akeyless configures this store to sync secrets using
                        Akeyless Vault provider
                      type: object
                      required:
                        - akeylessGWApiURL
                        - authSecretRef
                      properties:
                        akeylessGWApiURL:
                          description: >-
                            Akeyless GW API Url from which the secrets to be
                            fetched from.
                          type: string
                        authSecretRef:
                          description: >-
                            Auth configures how the operator authenticates with
                            Akeyless.
                          type: object
                          properties:
                            kubernetesAuth:
                              description: >-
                                Kubernetes authenticates with Akeyless by
                                passing the ServiceAccount token stored in the
                                named Secret resource.
                              type: object
                              required:
                                - accessID
                                - k8sConfName
                              properties:
                                accessID:
                                  description: >-
                                    the Akeyless Kubernetes auth-method
                                    access-id
                                  type: string
                                k8sConfName:
                                  description: >-
                                    Kubernetes-auth configuration name in
                                    Akeyless-Gateway
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional secret field containing a
                                    Kubernetes ServiceAccount JWT used for
                                    authenticating with Akeyless. If a name is
                                    specified without a key, `token` is the
                                    default. If one is not specified, the one
                                    bound to the controller will be used.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                serviceAccountRef:
                                  description: >-
                                    Optional service account field containing
                                    the name of a kubernetes ServiceAccount. If
                                    the service account is specified, the
                                    service account secret token JWT will be
                                    used for authenticating with Akeyless. If
                                    the service account selector is not
                                    supplied, the secretRef will be used
                                    instead.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            secretRef:
                              description: >-
                                Reference to a Secret that contains the details
                                to authenticate with Akeyless.
                              type: object
                              properties:
                                accessID:
                                  description: >-
                                    The SecretAccessID is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessType:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessTypeParam:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        caBundle:
                          description: >-
                            PEM/base64 encoded CA bundle used to validate
                            Akeyless Gateway certificate. Only used if the
                            AkeylessGWApiURL URL is using HTTPS protocol. If not
                            set the system root certificates are used to
                            validate the TLS connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Akeyless Gateway certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key where the CA certificate can be found in
                                the Secret or ConfigMap.
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: >-
                                The namespace the Provider type is in. Can only
                                be defined when used in a ClusterSecretStore.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                    alibaba:
                      description: >-
                        Alibaba configures this store to sync secrets using
                        Alibaba Cloud provider
                      type: object
                      required:
                        - auth
                        - regionID
                      properties:
                        auth:
                          description: AlibabaAuth contains a secretRef for credentials.
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              description: >-
                                AlibabaAuthSecretRef holds secret references for
                                Alibaba credentials.
                              type: object
                              required:
                                - accessKeyIDSecretRef
                                - accessKeySecretSecretRef
                              properties:
                                accessKeyIDSecretRef:
                                  description: The AccessKeyID is used for authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                accessKeySecretSecretRef:
                                  description: >-
                                    The AccessKeySecret is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        endpoint:
                          type: string
                        regionID:
                          description: Alibaba Region to be used for the provider
                          type: string
                    aws:
                      description: >-
                        AWS configures this store to sync secrets using AWS
                        Secret Manager provider
                      type: object
                      required:
                        - region
                        - service
                      properties:
                        additionalRoles:
                          description: >-
                            AdditionalRoles is a chained list of Role ARNs which
                            the SecretManager provider will sequentially assume
                            before assuming Role
                          type: array
                          items:
                            type: string
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against AWS if not set aws sdk will
                            infer credentials from your environment see:
                            https://docs.aws.amazon.com/sdk-for-go/v1/developer-guide/configuring-sdk.html#specifying-credentials
                          type: object
                          properties:
                            jwt:
                              description: >-
                                Authenticate against AWS using service account
                                tokens.
                              type: object
                              properties:
                                serviceAccountRef:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            secretRef:
                              description: >-
                                AWSAuthSecretRef holds secret references for AWS
                                credentials both AccessKeyID and SecretAccessKey
                                must be defined in order to properly
                                authenticate.
                              type: object
                              properties:
                                accessKeyIDSecretRef:
                                  description: The AccessKeyID is used for authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                secretAccessKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                sessionTokenSecretRef:
                                  description: >-
                                    The SessionToken used for authentication
                                    This must be defined if AccessKeyID and
                                    SecretAccessKey are temporary credentials
                                    see:
                                    https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_use-resources.html
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        region:
                          description: AWS Region to be used for the provider
                          type: string
                        role:
                          description: >-
                            Role is a Role ARN which the SecretManager provider
                            will assume
                          type: string
                        service:
                          description: >-
                            Service defines which service should be used to
                            fetch the secrets
                          type: string
                          enum:
                            - SecretsManager
                            - ParameterStore
                    azurekv:
                      description: >-
                        AzureKV configures this store to sync secrets using
                        Azure Key Vault provider
                      type: object
                      required:
                        - vaultUrl
                      properties:
                        authSecretRef:
                          description: >-
                            Auth configures how the operator authenticates with
                            Azure. Required for ServicePrincipal auth type.
                          type: object
                          properties:
                            clientId:
                              description: >-
                                The Azure clientId of the service principle used
                                for authentication.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            clientSecret:
                              description: >-
                                The Azure ClientSecret of the service principle
                                used for authentication.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        authType:
                          description: >-
                            Auth type defines how to authenticate to the
                            keyvault service. Valid values are: -
                            "ServicePrincipal" (default): Using a service
                            principal (tenantId, clientId, clientSecret) -
                            "ManagedIdentity": Using Managed Identity assigned
                            to the pod (see aad-pod-identity)
                          type: string
                          default: ServicePrincipal
                          enum:
                            - ServicePrincipal
                            - ManagedIdentity
                            - WorkloadIdentity
                        environmentType:
                          description: >-
                            EnvironmentType specifies the Azure cloud
                            environment endpoints to use for connecting and
                            authenticating with Azure. By default it points to
                            the public cloud AAD endpoint. The following
                            endpoints are available, also see here:
                            https://github.com/Azure/go-autorest/blob/main/autorest/azure/environments.go#L152
                            PublicCloud, USGovernmentCloud, ChinaCloud,
                            GermanCloud
                          type: string
                          default: PublicCloud
                          enum:
                            - PublicCloud
                            - USGovernmentCloud
                            - ChinaCloud
                            - GermanCloud
                        identityId:
                          description: >-
                            If multiple Managed Identity is assigned to the pod,
                            you can select the one to be used
                          type: string
                        serviceAccountRef:
                          description: >-
                            ServiceAccountRef specified the service account that
                            should be used when authenticating with
                            WorkloadIdentity.
                          type: object
                          required:
                            - name
                          properties:
                            audiences:
                              description: >-
                                Audience specifies the `aud` claim for the
                                service account token If the service account
                                uses a well-known annotation for e.g. IRSA or
                                GCP Workload Identity then this audiences will
                                be appended to the list
                              type: array
                              items:
                                type: string
                            name:
                              description: >-
                                The name of the ServiceAccount resource being
                                referred to.
                              type: string
                            namespace:
                              description: >-
                                Namespace of the resource being referred to.
                                Ignored if referent is not cluster-scoped.
                                cluster-scoped defaults to the namespace of the
                                referent.
                              type: string
                        tenantId:
                          description: >-
                            TenantID configures the Azure Tenant to send
                            requests to. Required for ServicePrincipal auth
                            type.
                          type: string
                        vaultUrl:
                          description: Vault Url from which the secrets to be fetched from.
                          type: string
                    doppler:
                      description: >-
                        Doppler configures this store to sync secrets using the
                        Doppler provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how the Operator authenticates with
                            the Doppler API
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              type: object
                              required:
                                - dopplerToken
                              properties:
                                dopplerToken:
                                  description: >-
                                    The DopplerToken is used for authentication.
                                    See
                                    https://docs.doppler.com/reference/api#authentication
                                    for auth token types. The Key attribute
                                    defaults to dopplerToken if not specified.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        config:
                          description: >-
                            Doppler config (required if not using a Service
                            Token)
                          type: string
                        format:
                          description: >-
                            Format enables the downloading of secrets as a file
                            (string)
                          type: string
                          enum:
                            - json
                            - dotnet-json
                            - env
                            - yaml
                            - docker
                        nameTransformer:
                          description: >-
                            Environment variable compatible name transforms that
                            change secret names to a different format
                          type: string
                          enum:
                            - upper-camel
                            - camel
                            - lower-snake
                            - tf-var
                            - dotnet-env
                        project:
                          description: >-
                            Doppler project (required if not using a Service
                            Token)
                          type: string
                    fake:
                      description: Fake configures a store with static key/value pairs
                      type: object
                      required:
                        - data
                      properties:
                        data:
                          type: array
                          items:
                            type: object
                            required:
                              - key
                            properties:
                              key:
                                type: string
                              value:
                                type: string
                              valueMap:
                                type: object
                                additionalProperties:
                                  type: string
                              version:
                                type: string
                    gcpsm:
                      description: >-
                        GCPSM configures this store to sync secrets using Google
                        Cloud Platform Secret Manager provider
                      type: object
                      properties:
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against GCP
                          type: object
                          properties:
                            secretRef:
                              type: object
                              properties:
                                secretAccessKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            workloadIdentity:
                              type: object
                              required:
                                - clusterLocation
                                - clusterName
                                - serviceAccountRef
                              properties:
                                clusterLocation:
                                  type: string
                                clusterName:
                                  type: string
                                clusterProjectID:
                                  type: string
                                serviceAccountRef:
                                  description: A reference to a ServiceAccount resource.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        projectID:
                          description: ProjectID project where secret is located
                          type: string
                    gitlab:
                      description: >-
                        Gitlab configures this store to sync secrets using
                        Gitlab Variables provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with a GitLab instance.
                          type: object
                          required:
                            - SecretRef
                          properties:
                            SecretRef:
                              type: object
                              properties:
                                accessToken:
                                  description: AccessToken is used for authentication.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        environment:
                          description: >-
                            Environment environment_scope of gitlab CI/CD
                            variables (Please see
                            https://docs.gitlab.com/ee/ci/environments/#create-a-static-environment
                            on how to create environments)
                          type: string
                        groupIDs:
                          description: >-
                            GroupIDs specify, which gitlab groups to pull
                            secrets from. Group secrets are read from left to
                            right followed by the project variables.
                          type: array
                          items:
                            type: string
                        inheritFromGroups:
                          description: >-
                            InheritFromGroups specifies whether parent groups
                            should be discovered and checked for secrets.
                          type: boolean
                        projectID:
                          description: >-
                            ProjectID specifies a project where secrets are
                            located.
                          type: string
                        url:
                          description: >-
                            URL configures the GitLab instance URL. Defaults to
                            https://gitlab.com/.
                          type: string
                    ibm:
                      description: >-
                        IBM configures this store to sync secrets using IBM
                        Cloud provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the IBM secrets manager.
                          type: object
                          maxProperties: 1
                          minProperties: 1
                          properties:
                            containerAuth:
                              description: >-
                                IBM Container-based auth with IAM Trusted
                                Profile.
                              type: object
                              required:
                                - profile
                              properties:
                                iamEndpoint:
                                  type: string
                                profile:
                                  description: the IBM Trusted Profile
                                  type: string
                                tokenLocation:
                                  description: Location the token is mounted on the pod
                                  type: string
                            secretRef:
                              type: object
                              properties:
                                secretApiKeySecretRef:
                                  description: >-
                                    The SecretAccessKey is used for
                                    authentication
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        serviceUrl:
                          description: >-
                            ServiceURL is the Endpoint URL that is specific to
                            the Secrets Manager service instance
                          type: string
                    keepersecurity:
                      description: >-
                        KeeperSecurity configures this store to sync secrets
                        using the KeeperSecurity provider
                      type: object
                      required:
                        - authRef
                        - folderID
                      properties:
                        authRef:
                          description: >-
                            A reference to a specific 'key' within a Secret
                            resource, In some instances, `key` is a required
                            field.
                          type: object
                          properties:
                            key:
                              description: >-
                                The key of the entry in the Secret resource's
                                `data` field to be used. Some instances of this
                                field may be defaulted, in others it may be
                                required.
                              type: string
                            name:
                              description: >-
                                The name of the Secret resource being referred
                                to.
                              type: string
                            namespace:
                              description: >-
                                Namespace of the resource being referred to.
                                Ignored if referent is not cluster-scoped.
                                cluster-scoped defaults to the namespace of the
                                referent.
                              type: string
                        folderID:
                          type: string
                    kubernetes:
                      description: >-
                        Kubernetes configures this store to sync secrets using a
                        Kubernetes cluster provider
                      type: object
                      required:
                        - auth
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with a Kubernetes instance.
                          type: object
                          maxProperties: 1
                          minProperties: 1
                          properties:
                            cert:
                              description: >-
                                has both clientCert and clientKey as
                                secretKeySelector
                              type: object
                              properties:
                                clientCert:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                clientKey:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            serviceAccount:
                              description: >-
                                points to a service account that should be used
                                for authentication
                              type: object
                              required:
                                - name
                              properties:
                                audiences:
                                  description: >-
                                    Audience specifies the `aud` claim for the
                                    service account token If the service account
                                    uses a well-known annotation for e.g. IRSA
                                    or GCP Workload Identity then this audiences
                                    will be appended to the list
                                  type: array
                                  items:
                                    type: string
                                name:
                                  description: >-
                                    The name of the ServiceAccount resource
                                    being referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            token:
                              description: use static token to authenticate with
                              type: object
                              properties:
                                bearerToken:
                                  description: >-
                                    A reference to a specific 'key' within a
                                    Secret resource, In some instances, `key` is
                                    a required field.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        remoteNamespace:
                          description: Remote namespace to fetch the secrets from
                          type: string
                          default: default
                        server:
                          description: configures the Kubernetes server Address.
                          type: object
                          properties:
                            caBundle:
                              description: CABundle is a base64-encoded CA certificate
                              type: string
                              format: byte
                            caProvider:
                              description: >-
                                see:
                                https://external-secrets.io/v0.4.1/spec/#external-secrets.io/v1alpha1.CAProvider
                              type: object
                              required:
                                - name
                                - type
                              properties:
                                key:
                                  description: >-
                                    The key where the CA certificate can be
                                    found in the Secret or ConfigMap.
                                  type: string
                                name:
                                  description: >-
                                    The name of the object located at the
                                    provider type.
                                  type: string
                                namespace:
                                  description: >-
                                    The namespace the Provider type is in. Can
                                    only be defined when used in a
                                    ClusterSecretStore.
                                  type: string
                                type:
                                  description: >-
                                    The type of provider to use such as
                                    "Secret", or "ConfigMap".
                                  type: string
                                  enum:
                                    - Secret
                                    - ConfigMap
                            url:
                              description: configures the Kubernetes server Address.
                              type: string
                              default: kubernetes.default
                    onepassword:
                      description: >-
                        OnePassword configures this store to sync secrets using
                        the 1Password Cloud provider
                      type: object
                      required:
                        - auth
                        - connectHost
                        - vaults
                      properties:
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against OnePassword Connect Server
                          type: object
                          required:
                            - secretRef
                          properties:
                            secretRef:
                              description: >-
                                OnePasswordAuthSecretRef holds secret references
                                for 1Password credentials.
                              type: object
                              required:
                                - connectTokenSecretRef
                              properties:
                                connectTokenSecretRef:
                                  description: >-
                                    The ConnectToken is used for authentication
                                    to a 1Password Connect Server.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                        connectHost:
                          description: >-
                            ConnectHost defines the OnePassword Connect Server
                            to connect to
                          type: string
                        vaults:
                          description: >-
                            Vaults defines which OnePassword vaults to search in
                            which order
                          type: object
                          additionalProperties:
                            type: integer
                    oracle:
                      description: >-
                        Oracle configures this store to sync secrets using
                        Oracle Vault provider
                      type: object
                      required:
                        - region
                        - vault
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the Oracle Vault. If empty, use the instance
                            principal, otherwise the user credentials specified
                            in Auth.
                          type: object
                          required:
                            - secretRef
                            - tenancy
                            - user
                          properties:
                            secretRef:
                              description: SecretRef to pass through sensitive information.
                              type: object
                              required:
                                - fingerprint
                                - privatekey
                              properties:
                                fingerprint:
                                  description: >-
                                    Fingerprint is the fingerprint of the API
                                    private key.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                privatekey:
                                  description: >-
                                    PrivateKey is the user's API Signing Key in
                                    PEM format, used for authentication.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            tenancy:
                              description: >-
                                Tenancy is the tenancy OCID where user is
                                located.
                              type: string
                            user:
                              description: User is an access OCID specific to the account.
                              type: string
                        region:
                          description: Region is the region where vault is located.
                          type: string
                        vault:
                          description: >-
                            Vault is the vault's OCID of the specific vault
                            where secret is located.
                          type: string
                    scaleway:
                      description: Scaleway
                      type: object
                      required:
                        - accessKey
                        - projectId
                        - region
                        - secretKey
                      properties:
                        accessKey:
                          description: AccessKey is the non-secret part of the api key.
                          type: object
                          properties:
                            secretRef:
                              description: >-
                                SecretRef references a key in a secret that will
                                be used as value.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            value:
                              description: >-
                                Value can be specified directly to set a value
                                without using a secret.
                              type: string
                        apiUrl:
                          description: >-
                            APIURL is the url of the api to use. Defaults to
                            https://api.scaleway.com
                          type: string
                        projectId:
                          description: >-
                            ProjectID is the id of your project, which you can
                            find in the console:
                            https://console.scaleway.com/project/settings
                          type: string
                        region:
                          description: >-
                            Region where your secrets are located:
                            https://developers.scaleway.com/en/quickstart/#region-and-zone
                          type: string
                        secretKey:
                          description: SecretKey is the non-secret part of the api key.
                          type: object
                          properties:
                            secretRef:
                              description: >-
                                SecretRef references a key in a secret that will
                                be used as value.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                            value:
                              description: >-
                                Value can be specified directly to set a value
                                without using a secret.
                              type: string
                    senhasegura:
                      description: >-
                        Senhasegura configures this store to sync secrets using
                        senhasegura provider
                      type: object
                      required:
                        - auth
                        - module
                        - url
                      properties:
                        auth:
                          description: >-
                            Auth defines parameters to authenticate in
                            senhasegura
                          type: object
                          required:
                            - clientId
                            - clientSecretSecretRef
                          properties:
                            clientId:
                              type: string
                            clientSecretSecretRef:
                              description: >-
                                A reference to a specific 'key' within a Secret
                                resource, In some instances, `key` is a required
                                field.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        ignoreSslCertificate:
                          description: >-
                            IgnoreSslCertificate defines if SSL certificate must
                            be ignored
                          type: boolean
                          default: false
                        module:
                          description: >-
                            Module defines which senhasegura module should be
                            used to get secrets
                          type: string
                        url:
                          description: URL of senhasegura
                          type: string
                    vault:
                      description: >-
                        Vault configures this store to sync secrets using Hashi
                        provider
                      type: object
                      required:
                        - auth
                        - server
                      properties:
                        auth:
                          description: >-
                            Auth configures how secret-manager authenticates
                            with the Vault server.
                          type: object
                          properties:
                            appRole:
                              description: >-
                                AppRole authenticates with Vault using the App
                                Role auth mechanism, with the role and secret
                                stored in a Kubernetes Secret resource.
                              type: object
                              required:
                                - path
                                - roleId
                                - secretRef
                              properties:
                                path:
                                  description: >-
                                    Path where the App Role authentication
                                    backend is mounted in Vault, e.g: "approle"
                                  type: string
                                  default: approle
                                roleId:
                                  description: >-
                                    RoleID configured in the App Role
                                    authentication backend when setting up the
                                    authentication backend in Vault.
                                  type: string
                                secretRef:
                                  description: >-
                                    Reference to a key in a Secret that contains
                                    the App Role secret used to authenticate
                                    with Vault. The `key` field must be
                                    specified and denotes which entry within the
                                    Secret resource is used as the app role
                                    secret.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            cert:
                              description: >-
                                Cert authenticates with TLS Certificates by
                                passing client certificate, private key and ca
                                certificate Cert authentication method
                              type: object
                              properties:
                                clientCert:
                                  description: >-
                                    ClientCert is a certificate to authenticate
                                    using the Cert Vault authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                secretRef:
                                  description: >-
                                    SecretRef to a key in a Secret resource
                                    containing client private key to
                                    authenticate with Vault using the Cert
                                    authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            jwt:
                              description: >-
                                Jwt authenticates with Vault by passing role and
                                JWT token using the JWT/OIDC authentication
                                method
                              type: object
                              required:
                                - path
                              properties:
                                kubernetesServiceAccountToken:
                                  description: >-
                                    Optional ServiceAccountToken specifies the
                                    Kubernetes service account for which to
                                    request a token for with the `TokenRequest`
                                    API.
                                  type: object
                                  required:
                                    - serviceAccountRef
                                  properties:
                                    audiences:
                                      description: >-
                                        Optional audiences field that will be
                                        used to request a temporary Kubernetes
                                        service account token for the service
                                        account referenced by
                                        `serviceAccountRef`. Defaults to a
                                        single audience `vault` it not
                                        specified. Deprecated: use
                                        serviceAccountRef.Audiences instead
                                      type: array
                                      items:
                                        type: string
                                    expirationSeconds:
                                      description: >-
                                        Optional expiration time in seconds that
                                        will be used to request a temporary
                                        Kubernetes service account token for the
                                        service account referenced by
                                        `serviceAccountRef`. Deprecated: this
                                        will be removed in the future. Defaults
                                        to 10 minutes.
                                      type: integer
                                      format: int64
                                    serviceAccountRef:
                                      description: >-
                                        Service account field containing the
                                        name of a kubernetes ServiceAccount.
                                      type: object
                                      required:
                                        - name
                                      properties:
                                        audiences:
                                          description: >-
                                            Audience specifies the `aud` claim for
                                            the service account token If the service
                                            account uses a well-known annotation for
                                            e.g. IRSA or GCP Workload Identity then
                                            this audiences will be appended to the
                                            list
                                          type: array
                                          items:
                                            type: string
                                        name:
                                          description: >-
                                            The name of the ServiceAccount resource
                                            being referred to.
                                          type: string
                                        namespace:
                                          description: >-
                                            Namespace of the resource being referred
                                            to. Ignored if referent is not
                                            cluster-scoped. cluster-scoped defaults
                                            to the namespace of the referent.
                                          type: string
                                path:
                                  description: >-
                                    Path where the JWT authentication backend is
                                    mounted in Vault, e.g: "jwt"
                                  type: string
                                  default: jwt
                                role:
                                  description: >-
                                    Role is a JWT role to authenticate using the
                                    JWT/OIDC Vault authentication method
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional SecretRef that refers to a key in a
                                    Secret resource containing JWT token to
                                    authenticate with Vault using the JWT/OIDC
                                    authentication method.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            kubernetes:
                              description: >-
                                Kubernetes authenticates with Vault by passing
                                the ServiceAccount token stored in the named
                                Secret resource to the Vault server.
                              type: object
                              required:
                                - mountPath
                                - role
                              properties:
                                mountPath:
                                  description: >-
                                    Path where the Kubernetes authentication
                                    backend is mounted in Vault, e.g:
                                    "kubernetes"
                                  type: string
                                  default: kubernetes
                                role:
                                  description: >-
                                    A required field containing the Vault Role
                                    to assume. A Role binds a Kubernetes
                                    ServiceAccount with a set of Vault policies.
                                  type: string
                                secretRef:
                                  description: >-
                                    Optional secret field containing a
                                    Kubernetes ServiceAccount JWT used for
                                    authenticating with Vault. If a name is
                                    specified without a key, `token` is the
                                    default. If one is not specified, the one
                                    bound to the controller will be used.
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                serviceAccountRef:
                                  description: >-
                                    Optional service account field containing
                                    the name of a kubernetes ServiceAccount. If
                                    the service account is specified, the
                                    service account secret token JWT will be
                                    used for authenticating with Vault. If the
                                    service account selector is not supplied,
                                    the secretRef will be used instead.
                                  type: object
                                  required:
                                    - name
                                  properties:
                                    audiences:
                                      description: >-
                                        Audience specifies the `aud` claim for
                                        the service account token If the service
                                        account uses a well-known annotation for
                                        e.g. IRSA or GCP Workload Identity then
                                        this audiences will be appended to the
                                        list
                                      type: array
                                      items:
                                        type: string
                                    name:
                                      description: >-
                                        The name of the ServiceAccount resource
                                        being referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                            ldap:
                              description: >-
                                Ldap authenticates with Vault by passing
                                username/password pair using the LDAP
                                authentication method
                              type: object
                              required:
                                - path
                                - username
                              properties:
                                path:
                                  description: >-
                                    Path where the LDAP authentication backend
                                    is mounted in Vault, e.g: "ldap"
                                  type: string
                                  default: ldap
                                secretRef:
                                  description: >-
                                    SecretRef to a key in a Secret resource
                                    containing password for the LDAP user used
                                    to authenticate with Vault using the LDAP
                                    authentication method
                                  type: object
                                  properties:
                                    key:
                                      description: >-
                                        The key of the entry in the Secret
                                        resource's `data` field to be used. Some
                                        instances of this field may be
                                        defaulted, in others it may be required.
                                      type: string
                                    name:
                                      description: >-
                                        The name of the Secret resource being
                                        referred to.
                                      type: string
                                    namespace:
                                      description: >-
                                        Namespace of the resource being referred
                                        to. Ignored if referent is not
                                        cluster-scoped. cluster-scoped defaults
                                        to the namespace of the referent.
                                      type: string
                                username:
                                  description: >-
                                    Username is a LDAP user name used to
                                    authenticate using the LDAP Vault
                                    authentication method
                                  type: string
                            tokenSecretRef:
                              description: >-
                                TokenSecretRef authenticates with Vault by
                                presenting a token.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caBundle:
                          description: >-
                            PEM encoded CA bundle used to validate Vault server
                            certificate. Only used if the Server URL is using
                            HTTPS protocol. This parameter is ignored for plain
                            HTTP protocol connection. If not set the system root
                            certificates are used to validate the TLS
                            connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Vault server certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key where the CA certificate can be found in
                                the Secret or ConfigMap.
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: >-
                                The namespace the Provider type is in. Can only
                                be defined when used in a ClusterSecretStore.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                        forwardInconsistent:
                          description: >-
                            ForwardInconsistent tells Vault to forward
                            read-after-write requests to the Vault leader
                            instead of simply retrying within a loop. This can
                            increase performance if the option is enabled
                            serverside.
                            https://www.vaultproject.io/docs/configuration/replication#allow_forwarding_via_header
                          type: boolean
                        namespace:
                          description: >-
                            Name of the vault namespace. Namespaces is a set of
                            features within Vault Enterprise that allows Vault
                            environments to support Secure Multi-tenancy. e.g:
                            "ns1". More about namespaces can be found here
                            https://www.vaultproject.io/docs/enterprise/namespaces
                          type: string
                        path:
                          description: >-
                            Path is the mount path of the Vault KV backend
                            endpoint, e.g: "secret". The v2 KV secret engine
                            version specific "/data" path suffix for fetching
                            secrets from Vault is optional and will be appended
                            if not present in specified path.
                          type: string
                        readYourWrites:
                          description: >-
                            ReadYourWrites ensures isolated read-after-write
                            semantics by providing discovered cluster
                            replication states in each request. More information
                            about eventual consistency in Vault can be found
                            here
                            https://www.vaultproject.io/docs/enterprise/consistency
                          type: boolean
                        server:
                          description: >-
                            Server is the connection address for the Vault
                            server, e.g: "https://vault.example.com:8200".
                          type: string
                        version:
                          description: >-
                            Version is the Vault KV secret engine version. This
                            can be either "v1" or "v2". Version defaults to
                            "v2".
                          type: string
                          default: v2
                          enum:
                            - v1
                            - v2
                    webhook:
                      description: >-
                        Webhook configures this store to sync secrets using a
                        generic templated webhook
                      type: object
                      required:
                        - result
                        - url
                      properties:
                        body:
                          description: Body
                          type: string
                        caBundle:
                          description: >-
                            PEM encoded CA bundle used to validate webhook
                            server certificate. Only used if the Server URL is
                            using HTTPS protocol. This parameter is ignored for
                            plain HTTP protocol connection. If not set the
                            system root certificates are used to validate the
                            TLS connection.
                          type: string
                          format: byte
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            webhook server certificate.
                          type: object
                          required:
                            - name
                            - type
                          properties:
                            key:
                              description: >-
                                The key the value inside of the provider type to
                                use, only used with "Secret" type
                              type: string
                            name:
                              description: >-
                                The name of the object located at the provider
                                type.
                              type: string
                            namespace:
                              description: The namespace the Provider type is in.
                              type: string
                            type:
                              description: >-
                                The type of provider to use such as "Secret", or
                                "ConfigMap".
                              type: string
                              enum:
                                - Secret
                                - ConfigMap
                        headers:
                          description: Headers
                          type: object
                          additionalProperties:
                            type: string
                        method:
                          description: Webhook Method
                          type: string
                        result:
                          description: Result formatting
                          type: object
                          properties:
                            jsonPath:
                              description: Json path of return value
                              type: string
                        secrets:
                          description: >-
                            Secrets to fill in templates These secrets will be
                            passed to the templating function as key value pairs
                            under the given name
                          type: array
                          items:
                            type: object
                            required:
                              - name
                              - secretRef
                            properties:
                              name:
                                description: Name of this secret in templates
                                type: string
                              secretRef:
                                description: Secret ref to fill in credentials
                                type: object
                                properties:
                                  key:
                                    description: >-
                                      The key of the entry in the Secret
                                      resource's `data` field to be used. Some
                                      instances of this field may be defaulted,
                                      in others it may be required.
                                    type: string
                                  name:
                                    description: >-
                                      The name of the Secret resource being
                                      referred to.
                                    type: string
                                  namespace:
                                    description: >-
                                      Namespace of the resource being referred
                                      to. Ignored if referent is not
                                      cluster-scoped. cluster-scoped defaults to
                                      the namespace of the referent.
                                    type: string
                        timeout:
                          description: Timeout
                          type: string
                        url:
                          description: Webhook url to call
                          type: string
                    yandexcertificatemanager:
                      description: >-
                        YandexCertificateManager configures this store to sync
                        secrets using Yandex Certificate Manager provider
                      type: object
                      required:
                        - auth
                      properties:
                        apiEndpoint:
                          description: >-
                            Yandex.Cloud API endpoint (e.g.
                            'api.cloud.yandex.net:443')
                          type: string
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against Yandex Certificate Manager
                          type: object
                          properties:
                            authorizedKeySecretRef:
                              description: The authorized key used for authentication
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Yandex.Cloud server certificate.
                          type: object
                          properties:
                            certSecretRef:
                              description: >-
                                A reference to a specific 'key' within a Secret
                                resource, In some instances, `key` is a required
                                field.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                    yandexlockbox:
                      description: >-
                        YandexLockbox configures this store to sync secrets
                        using Yandex Lockbox provider
                      type: object
                      required:
                        - auth
                      properties:
                        apiEndpoint:
                          description: >-
                            Yandex.Cloud API endpoint (e.g.
                            'api.cloud.yandex.net:443')
                          type: string
                        auth:
                          description: >-
                            Auth defines the information necessary to
                            authenticate against Yandex Lockbox
                          type: object
                          properties:
                            authorizedKeySecretRef:
                              description: The authorized key used for authentication
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                        caProvider:
                          description: >-
                            The provider for the CA bundle to use to validate
                            Yandex.Cloud server certificate.
                          type: object
                          properties:
                            certSecretRef:
                              description: >-
                                A reference to a specific 'key' within a Secret
                                resource, In some instances, `key` is a required
                                field.
                              type: object
                              properties:
                                key:
                                  description: >-
                                    The key of the entry in the Secret
                                    resource's `data` field to be used. Some
                                    instances of this field may be defaulted, in
                                    others it may be required.
                                  type: string
                                name:
                                  description: >-
                                    The name of the Secret resource being
                                    referred to.
                                  type: string
                                namespace:
                                  description: >-
                                    Namespace of the resource being referred to.
                                    Ignored if referent is not cluster-scoped.
                                    cluster-scoped defaults to the namespace of
                                    the referent.
                                  type: string
                refreshInterval:
                  description: >-
                    Used to configure store refresh interval in seconds. Empty
                    or 0 will default to the controller config.
                  type: integer
                retrySettings:
                  description: Used to configure http retries if failed
                  type: object
                  properties:
                    maxRetries:
                      type: integer
                      format: int32
                    retryInterval:
                      type: string
            status:
              description: SecretStoreStatus defines the observed state of the SecretStore.
              type: object
              properties:
                capabilities:
                  description: >-
                    SecretStoreCapabilities defines the possible operations a
                    SecretStore can do.
                  type: string
                conditions:
                  type: array
                  items:
                    type: object
                    required:
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        type: string
                        format: date-time
                      message:
                        type: string
                      reason:
                        type: string
                      status:
                        type: string
                      type:
                        type: string
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: AGE
          type: date
          jsonPath: .metadata.creationTimestamp
        - name: Status
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].reason
        - name: Capabilities
          type: string
          jsonPath: .status.capabilities
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          namespace: external-secrets
          name: external-secrets-external-secrets-webhook
          path: /convert
          port: 443
        caBundle: >-
          
      conversionReviewVersions:
        - v1
