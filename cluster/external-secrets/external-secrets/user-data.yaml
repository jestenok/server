apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: vault-example
  namespace: external-secrets
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: user-data
  data:
  - secretKey: username
    remoteRef:
      key: user-data
      property: username
  - secretKey: password
    remoteRef:
      key: user-data
      property: password